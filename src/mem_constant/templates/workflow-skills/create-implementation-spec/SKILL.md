---
name: create-implementation-spec
description: 'Create an implementation spec from scratch (acceptance criteria + test plan + standards adherence, not market-driven PRD). Use when the user says "lets create an implementation spec", "create a spec", or "create implementation requirements".'
---

Follow the instructions in ./workflow.md.
