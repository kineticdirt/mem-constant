# NYC Mafia × D&D — character sheets

Six-pillar design-doc sheets live here (`campaigns/_templates/character-sheet.md`).

- Indexed in Docs via `storyDirs` += `characters` (linuxbox-status CAMPAIGNS).
- Create from Hub Docs → **New character sheet** (or `POST /api/docs/character-sheet`).
- Do not put prose SoT only in `characters-registry.json`.
- Pixi RP `:8767` stays a separate product; engine dry-run: `python3 scripts/pixi/sheet-to-dossier.py --path …`
