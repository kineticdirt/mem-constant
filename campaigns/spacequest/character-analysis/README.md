# Moved — use `characters/`

Per-PC dossiers and analysis files now live under **`../characters/`**:

- **[[../characters/README]]** — index  
- **`../characters/pcs/`** — Cherish, Daji, Red, Aurora, Smolder, Geli  
- **`../characters/meta/`** — framework, ensemble, roundtable, backlog  

This folder is kept as a **stub** so old Obsidian links resolve. Prefer linking to `characters/` for new notes.
