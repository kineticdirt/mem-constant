# Copy to ~/.cloudflare/tableslop-tunnel.env on linuxbox (chmod 600).
# Zero Trust → Networks → Tunnels → WOD_HTR_LinBox_TABLESLOP → Install connector → token
#
# Public hostname (dashboard only — not this file):
#   map.tableslop.org → http://127.0.0.1:8765
TUNNEL_TOKEN=
