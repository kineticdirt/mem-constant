# Copy to ~/.cloudflare/abhinavall-tunnel.env on linuxbox (chmod 600).
# Zero Trust → Networks → Tunnels → abhinavall.net → Install connector → token
#
# Public hostname (dashboard only — not this file):
#   abhinavall.net → http://127.0.0.1:8780  (tunnel-origin-proxy → :3000 / :8790)
TUNNEL_TOKEN=
