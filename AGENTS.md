## Learned User Preferences

- **After every wrong / regression (required):** (1) **Understand why it was wrong** — root cause with evidence (not symptom patch, not “looks fixed”); name the mechanism that allowed it. (2) **Make sure we don’t do it again** — leave a durable prevention before closing: guard/assert/smoke, watermark, deploy-pair marker, CSS/grid invariant, lock, ledger lesson, or docs note that future agents will hit. A fix without prevention is unfinished. Same class as map-border wipes and Hub layout drift: silent reverts and PC≠potato skew are the usual vectors. **mem-constant:** SoT `docs/memory/ops-discipline.md` · pointer `.mem-constant/ops-discipline.md` · `mem-constant.yaml` `ops_discipline` · handoff §5 · Cursor rule `.cursor/rules/mem-constant.mdc` · **rewind** reads these at session start.
- **Pixi / engine design:** create **non-hardcoded permanent solutions which act dynamically** — package/`observed_world`/WORLD_DELTA first; mark leftovers `HARDCODE(scenario)` with an upgrade path; see `docs/pixi/CONTINUITY.md` §10. **Engine north star:** [`docs/pixi/ENGINE-GOBSTOPPER.md`](docs/pixi/ENGINE-GOBSTOPPER.md) (UI + peelable gobstopper layers; **delaying gratification** as pacing — agency/earned reveals, not instant wish-fulfillment; mem pointer `.mem-constant/pixi-engine.md`). **Nicknames/aliases** (e.g. Lin Mei ↔ Emily) must resolve to the **same canonical character sheet** (never fork aka into a second Cast card). Sheet UI/API must show **diegetic character data** (identity, age, clothing, established facts)—not telemetry/debug fields; ops/telemetry stays **chat-scoped** (message-tied or chat metadata), not on the sheet. **Sheets are design-doc grade** (an "uncomfortable" level of detail, no murkiness): describe clothing by **cut / fabric / brand / stitching** (shorts ≠ jeans) and apply that granularity to every field; **do not cap sheet panels by word count** (truncated "…" panels are unacceptable—cap only true disk bloat, never identity/state); present-cast sheets must stay **fully expanded/populated** after enrich (empty dossier / age-only stubs are a bug). Sheets/wiki must match the **session's diegetic clock** and **canonical age**—generated backstory/sexual history must not invent years or acts past current age (e.g. history continuing to 20 when age is 18); an early-scenario (e.g. Day-1) character must not surface advanced/future backstory (years-of-apocalypse scavenger lore) unless established in this thread or by an advanced clock. Keep a **canonical sexual-partner / body-count map** in SoT for reference so LLMs do not invent partners. **Life-stage age inference:** when role/education prose establishes a stage (e.g. US-path **college freshman**) and no stronger authored age exists, prefer biography-consistent bands (freshman most often **18**, sometimes 19)—correct locked invents that contradict the stage (e.g. freshman≠22); implement as **generic role-text rules**, never per-character/scenario hardcodes. **“Mentioned” / record gates** mean **epistemic first-hand relevance** (what they know, see, experience, or are told/expressed)—not bare name-string hits. **Cast registration** is global via WORLD_DELTA `new_people` / `new_edges`→ensure stubs (and related established cast), **not** scenario-package hardcodes for missing names. **Prose auto-salvage** must not mint junk/irrelevant scene-cast NPCs; keep related/established cast; prefer alias→canonical merge. Cast UI should separate **present** vs **introduced-but-not-present (off-screen)** from unmet/unplaced muddle. **Character agency:** cast should keep own goals and resist over-compliance — prefer immersion and gradual breakdown over obedient wish-fulfillment; when the player probes or asks in-fiction, NPCs should **make a decision / act**, not stall by answering with their own questions. **Diegetic clock:** time must advance between scenes; **manual time override** in UI is useful when auto-tick stalls. **Pre-generate** off-screen/other characters in the background so sheets are ready. **NSFW/smut is in-scope**: render explicit anatomy and sex acts accurately (no euphemisms like "inner thigh")—a **Venice** endpoint on linuxbox may act as an NSFW helper to generate/append explicit descriptions. **Docs↔Pixi sheet baseline:** authored sheets use six H2 pillars (Look / Speech / Act / Think / Skills+sexuality+else / Backstory) plus Identity header — plan `docs/plans/character-sheet-baseline-2026-07-27.md`, template `campaigns/_templates/character-sheet.md`; serialize into existing Pixi `body_profile` / `character_dossier` (no second schema); pillar-5 prose depth bar = campaign **Sasha** sheet ∪ Pixi’s richest dossier baseline. **Engine split:** Docs character authoring / beta-test on linuxbox uses **Pixi engine guts** (shared adapters/primitives); **Pixi RP product stays separate** (`linuxbox-pixi-rp` `:8767`) — share engine, not one UI. **Docs Beta purpose:** lighter Hub testbed (not `:8767`) for Pixi-shape experiments (scene quality, multi-turn, diegetic rules) and model guardrail/refusal probing before promoting patterns to live Pixi — background incremental work (`agents/DOCS_BETA_GUARDRAIL_TASK.md`), not an RP rewrite.
- **Multitask / shared state:** before mutating `characters-registry.json`, chat-threads, inbox, or SCP-ing those to potato — **acquire disk lock** (`bash scripts/linuxbox/multitask-lock.sh acquire chars-registry:tropic-gooner --holder <id> --wait`; files under `agents/state/multitask-locks/`; see `docs/multitask-shared-state-lock.md`), plus `AI_GROUPCHAT.md` Intent, pull/merge potato∪PC by id, respect registry `version` / `base_version` (see `docs/chars-registry-versioning.md` + `.cursor/rules/multitask-shared-state-checkin.mdc`). Never delete/hide side NPCs or blind-overwrite potato GM-created registry rows without merge + version check; soft-hide only true merge stubs with `canonical_id`.
- **Discord bot scripts:** any bot automation must read **`DISCORD_BOT_TOKEN`** (canonical). Live value on potato: hunter profile `~/.hermes/profiles/hunter-reckoning/.env` + tropic `campaigns/tropic-gooner/.env` (pointer comment in `~/.hermes/.env`). Do **not** leave an active `DISCORD_BOT_TOKEN=` in default `~/.hermes/.env` while both `hermes-gateway` (ops) and `hermes-gateway-hunter-reckoning` run — Hermes loads `.env` with override and ops crash-loops on the Discord token lock. **`DISCORD_TOKEN`** may remain as a compatibility alias where already wired — prefer **`DISCORD_BOT_TOKEN`** for new code. Do not invent other env names. **Model route 404s:** treat as possibly gone/nonexistent (not only transient); demote/skip for live ticks immediately; **do not delete** a model from options until **confirmed nonexistent on daily probes for over one week** — track in `agents/hermes-model-registry.json` `route_watchlist` / `route_404_policy`.
- Wants tooling kept on a **recurring upgrade schedule** (e.g. Claude Mem for Cursor) rather than set-and-forget; **gate every framework/package/library upgrade behind a supply-chain "pwned"/leak check** and upgrade only after confirming no compromise — auto-upgrade only when the check returns **SAFE**. Also wants automated testing (**Playwright** + tools) across lanes — **ops/UI complaints** (e.g. Chat not running) must be verified with **Playwright one-shot smokes** (`scripts/linuxbox/run-chat-ui-smoke.sh` → `reports/chat-ui-smoke/`; broader `run-dashboard-ui-smoke.sh`), **not curl-only** and not “blocked needs browser”; plus a recurring **code-discovery** lane surfacing interesting GitHub/web snippets to "stay in the know". **AwesomeFOSS** ([youtube.com/@AwesomeFOSS/shorts](https://www.youtube.com/@AwesomeFOSS/shorts)) = FOSS agent **capability options** catalog (e.g. Ego Lite)—need not implement all; when a ask matches a related short, **pose as a question** (“want to try X?”), never silent install/wire-in; prefer watch/note until yes; SAFE-gate / resource-governance (not potato crons lightly).
- When instructions could apply to more than one machine, state clearly whether they target this Windows desktop or the remote home linuxbox; **linuxbox drive/storage tasks mean remote potato setup — not archiving or moving the PC local workspace**; when diagnosing problems, run shell commands separately (not chained with `&&`) so each failure is visible. Prefers the agent to **run connectivity, installs, and setup in the terminal** when asked, not only describe commands. For multi-phase migrations involving irreversible operations (folder renames, deletes, persona/reframe rewrites), expects an **explicit hard-pause checkpoint between phases**; reversible work can batch into a single phase, but each destructive step needs its own gated review with the user before it runs.
- Wants the Tailscale **exit node** on **linuxbox only** (not this Windows PC); **Use exit node** per device — tailnet alone is not home-internet egress. **Exit-node internet black-hole:** if enabling linuxbox as exit node drops all client internet, **turn exit node off first** to recover (`tailscale set --exit-node=` / GUI Off); re-enable with **`--exit-node-allow-lan-access=true`** (and verify box IP forward/MASQUERADE) — runbook **`docs/remote-pc-setup/exit-node-internet-fix.md`**. **Home/away SSH handshake:** at home prefer **LAN** for potato (**Moonlight** + bare `ssh potato` / **`potato-lan`** via `Match exec` on `192.168.4.0/22` when reachable); Tailscale only when off the home network — keep Tailscale always on for MagicDNS/travel; wants a **usable same-Wi‑Fi/LAN fast path** (not docs-only) with **A/B smoke** vs Tailscale — decision report **`docs/remote-pc-setup/lan-vs-tailscale-decision.md`**. **Linuxbox wired `eth0`** is the verified home LAN path (**`192.168.4.59`**, 100 Mbit link, ~**70+ Mbit/s** real over SSH); **USB Wi‑Fi dongle** (RTL8188EUS, 2.4 GHz only) remains the wireless fallback (~**9 Mbit/s** practical ceiling). Before travel, validate remote tailnet from a **phone hotspot** (not home Wi‑Fi) using **`Test-RemoteTailnet.ps1`** (at **`E:\scripts\`**), and complete at-home-only steps: **Moonlight↔Apollo pairing** and **disable Tailscale key-expiry** (a key lapse mid-trip silently drops the device). **Hermes/OpenRouter** credentials belong in **`~/.hermes/.env`** only—not **`~/.bashrc`** or **`~/.local/bin/env`**. When secrets must be set on-box (OAuth client id/secret, tokens), give **paste-ready shell one-liners with placeholders** so the human inserts values locally—**never** ask them to paste secrets into the AI chat.
- **Portfolio redesign:** **v8-brutalist-map** is the baseline (over agent-built v4 fusion); **multi-page** site (not one long single page); experience and projects as **horizontal scroll timeline cards**; **v3-kinetic** palette/fonts with **old-net/cyberpunk** accents (deep-black chrome, yellow/cyan/lime highlights gradually—not full-page holographic backgrounds); localized **CRT**/**glass** ok; **retain the background watermark**; no Cequence-style chat labels; prototypes must mirror live **abhinavall.net** interactivity and **abhinavall.net**-style header branding (not placeholder “hm · portfolio”); duplicate header nav as footer **`hn-bar--footer`** (connect/top/jobs/blog/ask only—no Map/List in footer); Playwright smoke via **`.staging/portfolio-redesign/_screenshots/`** (`npm test`, optional **`PREVIEW_URL`** for linuxbox **`:8765`**). **Light mode** should be a **wholly separate aesthetic/components** site (same information well, surfaces different things)—kept as a **low-priority background task**.
- **Mobile / linuxbox agent visibility:** prefers **Pixel 3a** as light remote reader (minimal UI, **no Telegram**); **Tailscale + Chrome PWAs** and optional **JuiceSSH**; **admin dashboard** at **`https://abhinavall.net/Linuxbox/`** (Hub/Inbox/Reports/News/Campaigns/Chat/Tasks/Meta/Chars) as a **process-improvement hub** (multi-machine ops, not just status) behind **Cloudflare Access** (**Google SSO** preferred, **no Discord OAuth**) plus **Bitwarden-friendly** multi-account HTTP Basic (admin-role accounts e.g. **admin** / **HeadUser**; viewer-role e.g. **guest** / **viewer**—viewer = news/stocks/public docs only; credentials live only in potato `~/.linuxbox-dashboard/.env`) plus an **admin-generated temporary admin login/link** that **expires after 2 days** (temp credentials must grant **admin**, not viewer-only; baseline admin/viewer still work; runtime state under `agents/state/`, never commit secrets); **public shareable POC** at **`https://abhinavall.net/Intel/`** (no login, for showing others); wants **spend/token usage** on **Hub** (not Systems); treat Hub tabs as **silos** (Pixi RP; Mazda3 build; **map.tableslop.org** product — link out / do not embed map product as Hub SoT; Hub dark surfaces (Mazda3/garage parts links, etc.) must use **light link colors** — bare `<a>` UA navy is unreadable on black; Hub desktop grid: **`#mobile-page-select` must not steal column 1** (mobile-only control — known regression that collapses the main layout); News with expanded stocks; **Docs**; Hub **Chat ≠ Pixi RP** — talk-to-data / tasks / misconception fix, not roleplay; Tasks = command-and-control with rich panels; Meta TBD observability/philosophy); **Systems** = glance across silos + host/hardware (storage / CPU / RAM / **SoC temp** / **Eth link** / context — on Le Potato prefer live thermal+link over permanent Power/GPU N/A; **Task Manager–style** bars, **per-core** CPU, top processes/pods, what is consuming them) plus **expandable per-service mini-dashboards** (what each system does / how it works — not terminal-only) with stable short **ref/id names** on cards (e.g. `hub`, `tableslop`, `hermes`, `pixi`) for easy human/agent reference; **Docs** is a smart editor silo (not read-only): keep folder tree + search/smart filter; inline edit+save; highlight→comment / post-it reason edits on the page (limit popups); `@name` links for sheets/orgs/places/cultures; talk-to-doc; character **Beta-test** applies Pixi engine guts to one sheet — **player first-person** (I/me/my) and **character third-person** (name/she/he/they), not you-as-player or character-as-I; beta **scenario text must persist** across Run (not reset to default on Enter); Beta **Run Scene** needs an explicit **model dropdown** (incl. **DeepSeek**)—not a hidden default; Docs reader must **use available width** in split layout (no `68ch`/`--prose-max` skinny column leaving an empty middle gap beside comments); Docs covers campaigns **and** reports/ops notes (Obsidian-like utility) plus **Infranet** system scopes (`docs/infranet`, `projects/infranet`) in the same tree — show load progress/errors when the tree is slow; Hub **Worker Log** defaults to **Goal + live Current step** only — full think/worker log behind a clickable toggle; **low text density**, rendered markdown reports, human inbox — **inbox questions must ship rich `CONTEXT`/`context`** (in-fiction summary, what each option means; no bare acronyms like PRI-####), use **stable IDs**, and **never re-ask** items already in **`answered[]`** (or equivalent answered seeds)—**answered ≠ merely consumed**: consume may clear `open[]`, but answered ids must stay closed and must not be re-seeded; Inbox copy follows **`agents/INBOX_PROSE.md`** (human cadence — ban antithesis “It’s not X or Y, it’s Z” / fast-paced boilerplate); Hermes must **never** overwrite `human-inbox.json` as a bare JSON array (drops `answered[]`); **`scripts/linuxbox/human-inbox-normalize.py`** + **`consume-inbox-answers.py`** on the sync tick keep answered seeds closed; **Tasks**/**Hub** **Active now** / **Running now** should show in-flight ops (open user-tasks, lane focus, pods, chat jobs)—**not campaign-only** and **visible outside Hub**; prefer a **dense detail-rich grid** (fill the panel—not a sparse one-row strip over empty black) where each cell shows **what was thought/done**, not bare pod id/`think`; think/pod status colors: **red**=failed, **orange**=in progress, **light green**=tick/item ok, **dark green**=whole task succeeded; post-job Hub refresh should be snappy (~**≤10s**, not multi-minute waits); **do not poll-refresh/re-render focused text inputs** while the user is typing or selecting (known “ticking reset” on Chat log, bugs fields, Tasks Goal enter); **Hub/Meta last-run** should answer **What** (human lane/task) · **When** (ET) · **Outcome** (ok/IDLE/fail), and Meta should explain what it does (not opaque pod ids alone); Chat mobile **Threads** drawer at **≤720px**; **Chars** roster should show **faces/portraits** (not initials-only) so characters are distinguishable — support **multiple images per character**, primary pick/upload (file and image URL), **unretire**, in-card edit/click-through, **side characters / NPC relationship graphs**, and a **Merge characters** UI (soft-hide stubs via `canonical_id`; never hard-delete without ask); portrait “ugliness” is usually the **on-disk source JPEG** already — dashboard streams gallery files **as-is** (no adaptive network recompression); unresolved Discord attachments need an actionable **Resolve** popout (local `discord-export/**/attachments/` first, Discord fetch only if still missing) with a **Delete** option to remove a gallery image (not only pick/upload/resolve); honor explicit empty galleries and **do not** basename-steal another character’s portrait dir (wrong roommate/partner art is a known failure mode); **Stories** Discord character sheets must use full reader width and render sheet markdown (not raw fenced monospace); use real campaign assets (e.g. `characters/portraits/`, Character Images archives), **do not invent AI faces** unless asked; **never hard-delete GM/user-created Chars** (soft-hide/`canonical_id` only; ask before merge/delete); **ask before merging/deleting** duplicate registry entries; **Wholesome Boi** = the user (**GM**); **tableslop owner bootstrap** (`TABLESLOP_OWNER_DISCORD_ID`) resolves to Discord **`wholesome.man`** / display **WHOLESOMEest Boi** (The GM) — a **`dabutvin100`** login is a non-owner **`user`**-role account, not the GM owner, unless the user explicitly switches/grants it; **ad-hoc user tasks** via **`agents/user-tasks.json`** (v2: optional **`projects[]`** grouping + **`tasks[]`**) independent of campaign lanes (optional context attach); full IDE via **Cursor Remote SSH** to `~/agent-dump`; prefer **key-based SSH** (`id_rsa_potato`) over Tailscale SSH browser check (`sudo tailscale set --ssh=false` on linuxbox).
- **Resource governance + ponytail + loops:** optimizes **search tokens**, **memory**, **free vs paid models**, and **correctness** (`.cursor/rules/resource-governance.mdc`; linuxbox mirror **`CLAUDE.md`**); [ponytail](https://github.com/DietrichGebert/ponytail) YAGNI ladder (`.cursor/rules/ponytail.mdc` + skill; **`PONYTAIL_CLEANUP_BOARD.md`** fix/refine only — no deletions); when work parallelizes prefer **2–3 concurrent agents / multisubagents** — **one paid at a time**, free models until rate/limit caps; for a **singular one-off Task subagent** (incl. a parallel supervisor agent) prefer **Kimi K3** (`kimi-k3-max`); **Hermes/think paid only under C8 (two scenarios):** free pool exhausted, OR verified free failure against an explicit success metric (≥N free harness fails)—never pay on model self-claim; **loop engineering** interest = trace→judge→fix harness/evals over prompter loops — **Meta-Harness** ([stanford-iris-lab/meta-harness](https://github.com/stanford-iris-lab/meta-harness)) integrated at **`agents/meta-harness/`** + **`scripts/meta-harness/`** (pod traces, eval scoring, candidate promotion; upstream clone **outside** repo via **`setup-pc.sh`**); watch TED-RAG, don't integrate until mature. **Ops failure taxonomy:** when ticks/blocks fail, **categorize** the specific issue, have the agent **fill a structured form before ending** and log it; **similar recurring reports escalate** importance/severity — cleanup tasks for these matter. **Papercuts:** agents/lanes log friction as `pc-*` entries in **`agents/papercuts.md`** (usage `docs/agents/papercuts.md`) instead of only telling the user problems in chat — find problems quickly, resolve autonomously. **Cursor away-from-PC:** prefer **Cloud Agents / Automations** (`cursor.com/agents`) pointed at the **Linuxbox** repo so work continues without the desktop IDE online (phone for review); Hermes stays free always-on; **do not** put Cursor CLI on high-frequency potato crons — plan **`docs/plans/cursor-auto-away-from-pc-2026-07-29.md`**. Expects published memory tooling via **install path + CLI scaffold** (`pip install` then `mem-constant init`), not `git clone` alone; implementation end-to-end unless asked to pause. **Potato vs laptop:** linuxbox should stay **deterministic ops-light** (scripts/crons/health, minimal LLM overhead); **interactive AI** belongs on the **laptop/PC** — **maintain function at all costs** (improve efficiency; do **not** cut lanes/capabilities without human OK). **Pixi RP / Bonsai split:** Pixi RP is **OpenRouter-only** (no local GGUF/Bonsai for Send, NSFW routing, or coercion); always-on on linuxbox via **`linuxbox-pixi-rp`** at **`:8767`** (`~/pixi-rp/ObsidianWriterStack`, env `~/.linuxbox-pixi/deckard-local.env`) — Tailscale/LAN only, **not** on public abhinavall.net; **Pixi OpenRouter key stays in the Pixi env** (separate from Hermes/ops `~/.hermes/.env`) — do not share one key across RP and think; PC Pixi stack remains optional (dev tree often under **`ObsidianWriterStack/PixiApp/chat-ui`**). **Free-first Send:** main FG **Poolside Laguna** (`poolside/laguna-xs-2.1:free`), free fallback (e.g. Qwen `:free`); keep paid/alt models (DeepSeek, **ZenMux** **`moonshotai/kimi-k3-free`**, etc.) in the **model preset dropdown** for explicit choice, but do **not** auto-fall through to paid while credits are gone (`CHAT_UI_ALLOW_PAID_FALLBACK=0` until user re-enables); **explicit picks stick** — never silently re-route a chosen model (e.g. K3/DeepSeek) back to Laguna, and never force Laguna (or any free default) over what the user selected; **per-step model routing** — UI should let the user pick which model runs each pipeline step (Send, regen, Satyr stub, etc.), not one global forced default. **Per-model profiles:** each picker model needs its own data-driven setup (`turn_max_tokens`, prompt caps, hygiene/continuation quirks)—not one global turn max for every OpenRouter id. **Core product:** multi-character cast tracking + durable memory/WORLD_DELTA continuity (all Present NPCs stay in the beat — not single-speaker tunnel); **context inject** should prefer **latest scene + scene/chat summaries + in-scene character wiki stubs** (optional PC Satyr summarizer) over stuffing the full transcript; **Satyr stubs** are separate from full wiki but **injected the same way** into the OpenRouter prompt; harder **scene delineations** when cast separates; **object / character / event permanence** (physical descriptions, inventory, objects, personality/persona) must actually land in SoT and **Send inject** (dormant permanence code that never reaches the model is a bug; list inventories must merge, not wipe); **between posts** fully expand character continuity (rich sheets/wiki, not stubs or semantic-search-only scraps); **diegetic time** must advance correctly between scenes; **~6 presence tags** (present / off_scene / unmet / elsewhere / dead / exiled) so unmet/dead/exiled cast are never injected like on-screen speakers; **knowledge bounds / epistemic fog** for non-present cast; **dynamic sheets** that persist clothing and established facts (age, identity, places/objects)—scenario package seeds must not clobber sheet-established truth; **clothing/physical continuity** must hold through acts (no skip-undress / assumed-off); **Docs beta must not thin** the continuity/permanence path vs full Pixi Send; **place/spatial continuity** is place-generic (apartment/room/house/area—not apartment-hardcoded) and may subdivide large spaces; **alias/nickname → canonical sheet** resolution so alternate names do not fork identity; **never merge distinct cast** via fuzzy alias heuristics (e.g. Vivian ≠ Yuna—mature ice-queen vs college freshman are separate sheets); sheet surfaces show **diegetic facts**, not telemetry; scenario start cast/relations from the **scenario package** (no hardcoded bleed from a prior chat/session); relation/graph edges derived from package + observed world (not scenario-hardcoded stubs). **RP language:** keep narrative replies **English-focused** (no random foreign-language inserts unless diegetically requested); force providers (e.g. DeepSeek) to answer in **English** when they drift into Mandarin/Japanese/etc. **Seamless Send:** incomplete / cut-off RP replies are a product bug—recoverable `finish_reason=length` / hygiene strips should auto-continue or salvage—never leave the player babysitting `empty_assistant_content` / `CHAT_UI_RESPONSE_HYGIENE=0` as the product answer; **chat/scenario state is server-persisted** so mobile and PC share the same thread and leaving mid-Send does not drop the pipeline; **move turn-pipeline ownership server-side** (authoritative server WORLD_DELTA merge into `observed_world`, plus a **turn-job API** where the client kicks a job → polls status → reloads) — persisted state alone is not enough, since the provisional write only covers the primary hop, not the full continuation/quality/footer repair chain, so Send/Regenerate must survive a phone tab closing; **Regenerate must REPLACE the assistant turn, not append a new one**. **Mobile Pixi:** UI must work on phone (verify with Playwright mobile emulation), not desktop-only — prefer a **simplified primary RP surface** plus a **nested menu-of-menus** for advanced controls (do not squeeze the full desktop chrome into a tiny readable strip). **Regenerate:** if the last turn is the user, generate without deleting history; if the last turn was assistant, replace that reply only — discarded text goes to a hidden regen system layer, **never** into Turn guidance (Turn guidance = optional short user notes only); regen must **not** word-for-word repeat the discarded reply; **regen/response hygiene** must strip player **[Options]** menus, meta/OOC apologies, and stream-of-consciousness dumps—clean diegetic narrative only. **Speech vs narration:** quoted player text = diegetic PC speech; unquoted = narration/authorial direction (including unquoted description-probes — not questions posed to NPCs). **Composer parentheses:** anything in `(...)` in the user message is **Turn guidance / OOC**, not diegetic prose—parse out on Send/Edit and route to the guidance channel. **Hard regen** (distinct from Regenerate): delete last assistant, let the user edit the last user turn (parens→guidance), then **Send** as a fresh generation—not soft regen/anti-echo against the discarded reply. Prefer the newer laptop/GitHub Pixi design line over ad-hoc potato symptom patches (dynamic/data-driven, not hardcoded); **HTTPS on `:8767` preferred** for mobile PWA / Secure Context (Tailscale Serve or local TLS on Tailscale/LAN only — still **not** public Cloudflare). **Bonsai-27B** on desktop GPU (`:8000`, target **~262k ctx** with 4-bit KV) is an **optional** Hermes provider when the PC is up (`pc-bonsai-failover.sh` / `agents/pc-bonsai-routing.json`) — **not** required; Hermes must still run via **OpenRouter** and **ZenMux** (keys in `~/.hermes/.env`) when Bonsai is down. Potato never loads 27B weights. **Person/scene summarizer host order (potato Pixi):** if `SATYR_BASE_URL` / Tailscale PC Satyr (`:8001`) reachable use it; else **OpenRouter** cheap/free on potato; else deterministic hydrate — never load Satyr GGUF on potato. **PC Satyr** (`Satyr-V0.1-4B` **Q8_0** GGUF, separate port from Bonsai) remains optional PC-side capacity for scenes/wiki stubs — product path must still work when Satyr is down/unwired.
- Treats **linuxbox `~/agent-dump` as the canonical runtime workspace** and wants the box kept the **furthest along** — move linuxbox-related files over to potato rather than letting the PC tree lead (lean deploy; PC tree is a heavier dev mirror — prefer **minimizing PC clutter by archiving to linuxbox** `/mnt/archive` / box-side artifacts; **PC Pixi/ObsidianWriterStack retirement:** sync deltas to potato `~/pixi-rp` first, then verified compressed archive on spinning HDD **`/mnt/archive`** (not USB PERSONAL/SD) before any PC delete — **do not delete** until checksum-verified; laptop wipe only if reachable (often USB-kit-only). Only **`/mnt/archive/usb-shuttle/`** E: kit archives so far besides in-progress Pixi retire). After linuxbox-facing work run **`bash scripts/pc/push-linuxbox.sh --finished`** (bundles in **`agents/linuxbox-deploy-manifest.json`**; subagent **`.cursor/agents/linuxbox-push.md`**; post-deploy **`scripts/pc/fix-sh-crlf-remote.sh`** strips CRLF from remote **`scripts/linuxbox/*.sh`** and restores **`+x`**); after **Linuxbox** `main` git push also **`bash scripts/pc/push-linuxbox-git-bundle.sh`** (box can't HTTPS-pull private repo). **Auto lanes/deploys must never silently revert runtime state or dashboard features** — switching to always-on Hermes must not wipe registries, `agents/state/**`, or deployed UI unless explicitly asked; **differentiate human/GM Cursor work from agent output** — do not backtrack or undo what the human just implemented; enforce via **`agents/protected-runtime-paths.json`** + **`scripts/linuxbox/protected-paths.py`** + **`docs/runtime-state-protection.md`** (preserve/restore around `git reset --hard`, tarball filter on `push-linuxbox.sh`, **`verify-runtime-state.sh`** fail-loud gate) — include **`agents/model-budget/chat-catalog.json`** so free-tier Hub Chat Models rows are not clobbered to paid-only by sync/reset — Hub Models **Save must not truncate** the free rotate pool to 2 slots; do not let potato sunset **catalog v1** poison win over current free/paid SoT. **tableslop:** iterate on **PC** (`projects/tableslop/design/` preview **`:8767`**); **do not** push after every change — **`scripts/linuxbox/push-tableslop-map.sh`** (and server deploy) only at **milestones or handoff**; map binaries not git pull alone; **GM-drawn borders are sacred** — **never clear**/empty `regions-ui.json` polys or replace with stubs/ellipses/ovals without GM ask (known wipe: committed `sync-overlay-coords.mjs` ellipse template + git reset/checkout of the tracked runtime file); **potato owns** `regions-ui.json`; **GM v19 (371 verts) is committed to HEAD** (`25106e5`) as the tracked floor so ellipse stubs can never win a reset again, with **`skip-worktree`** on the file (PC+potato) shielding live GM polys from checkout/reset — re-apply skip-worktree if a bundle apply clears it; refuse deploy/SCP/`sync-overlay` when local is empty/ellipse-only while remote has GM verts; after `git reset --hard` / bundle apply, **restore GM-richer preserve** over git ellipse stubs (never leave stub on disk); after map work **verify** GM vert counts and fail loud if polys disappear (`campaigns/tropic-gooner/map/REGIONS-UI-LOCK.md`, `tableslop-gm-borders-guard.sh`, `verify-runtime-state`); HUD **Save border** persists; **Draw UX:** snap to **neighbor polygon vertices** (not decorative black art dots); **Load border** to edit an existing poly; region **dropdown syncs** when the map/legend selection changes; **cities have sub-regions** (draw parent region first, then nested city subzones); **Areas** polygons ON by default (or obvious toggle) with visible stroke/fill — pins-only with invisible borders is a bug; region-picker must not hard-block on “Unsaved verts belong to other region” — offer **rebind verts → selected region** / clear / cancel so GM can Save Porto (etc.) without discarding work.
- **North star + comms:** build **AI-native** solutions **cheaply and efficiently** — ship working things with **prior plan/understanding**, not reactive symptom patches. **Self-improvement of this AI/agent stack is tantamount** (Meta-Harness, evals, durable memory, Dashboard Chat→SoT for project/build facts e.g. mazda3 `parts.json`—not only thread text, **Agent coding** that edits+verifies on-box—not propose-only; train/test the stack on itself)—product lanes run under that priority, not instead of it: **Pixi RP** (`:8767`) Gobstopper engine — **mystery is exciting, confusion is frustrating** (`docs/pixi/ENGINE-GOBSTOPPER.md`); **tableslop** (`map.tableslop.org`) toward a **full game**, **abhinavall.net** as an in-depth **portfolio/blog** (skill-demo projects), and **X bookmarks research** (~every **3–4 days** / weekly floor; **PC logged-in**, **no X API**, ask login when needed—not per-post routing; **candy dump** first → `reports/x-bookmarks/` raw JSON + light keep/maybe/junk, deep parse later; **Cursor IDE browser ≠ Chrome**—login in the IDE browser session when scraping). **tableslop v1** = map+regions+cast+**session/Discord** play loop — prefer **cast/characters on `map.tableslop.org`** (combine/move off linuxbox-only Chars where possible; preserve registry data)—**Cast** is a **separate red dashboard/page** (not only a HUD chip; PC Cast app-view parity with mobile), with **preset character templates** + iterative follow-up questions, **undo/versioning** (git-like), and object/place linking; move **Chars/Stories** for tropic-gooner/Isla Primavera into the map **`worldeditor`** / HUD panels (map stays the product screen — **no full-screen dashboard takeover**; **admin/owner** get world-edit options as HUD panels/overlays; **observer/`user`** = view + own PC only — no border/place/SoT edit chrome) — **but the character-editing surface is a wholly separate dedicated page from the map, not a HUD/panel overlay** (solely for modifying characters: larger/readable presentation, World-of-Darkness fit, additions, notes); Discord OAuth/login required for edit; **canonical interactive map is `https://map.tableslop.org/`** — Hub **Map** tab must not act as the product (link/redirect there; do not leave a broken baked overlay as if it were SoT); map **green lines** = **highways/freeways** (traffic prediction); region border/fill colors should **match the city** aesthetic when styling. **Campaign availability / player trackers** belong on **`https://campaigns.tableslop.org/`** (potato `linuxbox-campaigns-avail` **`:8768`**) — separate from the map app; prefer extending existing **`cloudflared-tableslop`** with that hostname (not a second tunnel); until DNS is live use interim **`https://map.tableslop.org/camp/`**; never advertise bare **`potato:`** / LAN-only hosts as the player URL. **Tableslop dual-app:** campaigns host = Discord-linked availability/scheduling/cancellation + player meta and **PC↔character linking** (**Euro Adventure 2026 D&D** + NYC portals); map host = **Isla Primavera / Hunter / WoD** full-game dashboard (encounters, FOW, zones/streets, autonomous off-line sim—phased). **Tableslop HUD apps (map):** **Phone** (call **and** text), **Radio**, and **Sim** expand over the cast/info panel — orange inactive chips → greenish turquoise when active; **Cast** sits at the **end** of that strip; panel sits on the **right** and must be **resizable / pull-out** (not a fixed strip) and the Phone shell should **fill the dock**. Phone is a **multi-app HOME launcher** (icons + labels) — Contacts / Texts / Phone / Recents / Settings as separate apps, plus Island apps (WcDonalds, IsleMart/Primazon-class commerce, etc.); Texts is its own app, not only a softkey. Other in-world apps belong there too (food/product ordering — WcDonalds, off-brand Walmart/Target/Amazon); **firearms** only via physical storefronts characters must visit. Soft sim clock ~**48h IRL ≈ 1 world day** (subject to change). Scale ~**10 users** / 2–3 concurrent on **potato** for now; **laptop later** for heavy. Prefer **deterministic pregen** (city/parts) then AI; ship **Setup→Beta (S0–S3)** before launch overhead; finish **Isla Primavera** worldbuilding. **Focus cities first** (maps, lore bible, stories/groups, then expand): **Paradise** (`r01-paradise`), **Porto Lujara** (`r02-porto-lujuria`), **Jackedsonville** (marker; region **Crimson Quay** `r03-crimson-quay`). Aim for a **needlessly complicated semi-realistic** living world (radio/themed stations, in-game news, logistics/shipping, market/gunplay sim, social/spam noise, laws/norms/psychology, Hunter enemy/prep/cursed/supernatural layers where the weird stays hard to find unless sought)—**implement the code even when potato hardware cannot run it all yet**. **Region boundaries** also feed **procedural 3D city blocks** (client-side three.js; American Hawaii/Cuba pastel aesthetic) — **3D is a map toggle/overlay on the 2D map** (Google Maps–style), not a separate world; **player characters exist on the map** and run routines (drive / walk / bus). Auto-generated places, population projection, and full asset-class density should make cities feel **lived-in and vast** (almost too much). **City/district layouts** are plan-driven (**voronoi** / **strip** / **radial** as fits the place)—**not** always a spoke-and-wheel; when splits are unclear, **ask the GM** rather than inventing bad partitions. **Isla Primavera / tropic-gooner authoring:** quick character edit plus typed wiki links (prefer `[[kind: label]]`, e.g. school/place/year) across **Docs + Chars + Cast**; quest / place / character-question **makers** so the setting is authored like a **game engine**. **Map Discord accounts / OAuth:** public can view; **login required to edit** (and for **`worldeditor`**); roles **`owner`** / **`admin`** / **`user`** (owner from `TABLESLOP_OWNER_DISCORD_ID`); SQLite for users/tokens—not “optional OAuth later.” Wire Discord OAuth on **linuxbox** for `map.tableslop.org` (client id/secret on-box only). Plan: `docs/plans/tableslop-dual-app-roadmap-2026-08-01.md`. Want **per-campaign** portals (**Euro Adventure 2026 D&D** at `campaigns/euro-adventure-2026/` + NYC) with Discord-backed **availability/scheduling**, **chat/thread tracking**, plus PC/inventory tracking — whole tableslop stack runs on **linuxbox**; no “fixed/up” claim without potato curl evidence. **Discord campaign analysis:** AI should analyze Discord campaign traffic properly and support **name lookup** across exports/registries (e.g. Nelly, Toga, Red); **Euro Adventure 2026 D&D** is a **separate** Discord campaign from Tropic/Hunter and NYC (own tree under `campaigns/euro-adventure-2026/`). **DMs are not tracked today** (probe/export/ingest cover guild channels/threads and some player `*-private` **guild** channels only — not user↔user DMs; bots cannot read those without user-side export). Character-specific facts often live in DMs — treat DM coverage as a future opt-in gap, not as current capability. **Place names + pin coords must stay linked** (label position = marker); pins should auto-associate to regions by **point-in-polygon** (inside area → that region); labeled art **`vibes.png`** (`Obsidian/DND Archive/World Of Darkness/vibes.png`) is the **visual/name SoT** over lore invents—reconcile `map.json` / regions / campaign markdown to those spellings and keep wrong old names only as `aliases`; **GM draws region borders** (pins first; remove/hide auto/digitized borders until GM hand-draws city-based boundaries—do not keep agent ellipses as product SoT); hierarchy **island region → city → sub-regions** (draw parent first); map feedback via highlight/paste screenshot is welcome. **`wb-tg-factions` locked:** former PCs are ambient NPCs (people you might run into)—**no quest-obligation favor anchors** (favors may emerge in play later; do not invent PC favor debts). Prefer **asking follow-up / Inbox guidance** when blocked over guessing or idle LLM thrash; do not sit idle for hours with nothing to do. Dashboard/ops **errors should be investigated** (root-cause tasks), not only self-heal smoke tests; wants **additional News/Intel data sources** as a **world current-events / breaking-news** channel (global crawl—not tech-RSS-only or BBC-dominated). Public **Medium-style** essays need real technical depth and **no internal infra** in bodies; other articles/papers are **tool candidates**: analyze, trial, **self-apply on this stack**, keep what measurably helps. Answer **why they're asking** first; direct, clear, no saccharine AI tone (**.cursor/rules/anti-slop.mdc** + **`agents/AI_BAD_HABITS.md`** / **`.cursor/rules/ai-bad-habits.mdc`** — six bans: synonym rotation, hedging, frozen verbs, marketing adjectives, run-ons, phrasal filler; STE-aligned); explain in **plain technical English** (~90%) without assuming minutiae — generally, not only in inbox questions. Set concrete goals and verify; do not pad. **Campaign roles:** user is **GM**; AI is **scribe/implementer** — dashboard **Brainstorm**, not "Talk to GM"; **RP/worldbuilding** uses org **rise/fall phases** (not fill-in templates) with **nuance + why-chain context**; for setting forks **define choices in detail before the user picks**; AI should **engage the thought experiment** (justifications, broad strokes before details, self-encapsulated stories)—not bullet lists alone. **NYC Mafia × D&D** (when active / SoT `campaigns/nyc-mafia-dnd/`): **D&D 5e** after ~**300y** slow industrialization (Renaissance→late-1800s spell-tech); **1950s–60s** NYC/Jersey culture (jazz, instruments, nightlife, alcohol/drugs, street stereotypes) on an **uneven era stack** — **no exact calendar year** (not “1958”); **Gilded Age** industrialists + **magitech trains**; diegetic news/photos via **scry-press**/projection (not chemical photography); worldbuilding prose should sound **human/rambling** (street stereotypes OK—playable texture, Barry-dry), not AI-smooth; **capability asymmetry** (strong healing/life magic and automata/artificial life can run ahead; **computation** stays woefully weak); **not** 1931 Prohibition-magitech and **not** magic painted on the modern world; tech grows from **D&D spells/casting** (needs→magic iteration), steampunk-esque early-industrial; **capsule arms** (mana pulse / kinetic sling) — **no gunpowder**; multicultural peoples↔**5e ancestry** map (`SETTING-PEOPLES-RACES.md`); **automata as existential threat**; urban lock — cities dominate, **fringe** vampires/werewolves, druids framed as gov **terrorists**, **sprawl**/low-rise not steel towers, **subways** + **slow motor-carriages**, no big interstates yet; humor/tone compass = HBO **Barry**; Hub **Docs** surfaces `SETTING*` / `worldbuilding/` (concrete city build, not session stubs); superseded drafts → campaign **`_trash/`** (soft archive, no hard-delete); **major worldbuilding strokes before city flesh**; no Discord dependency for worldbuilding SoT when locked; optional parallel potato **`cursor:auto`** lane for NYC drafts while Hermes think covers other lanes. **Continuous content lanes** are **campaign · project · research · education** (ops/reliability still runs on its hard priority track). **Campaigns and projects are equal priority** (continuous RR — discover open work, measurable completion criteria, execute the plan); keep **portfolio** (`abhinavall.net`) and **News/Intel** coverage/quality as **active continuous** lanes (not idle); when all task boards are finished, **generate new testable tasks** (Playwright-verifiable) rather than stalling idle. **Research** = studies/benchmarks (free OpenRouter endpoints, e.g. Nemotron 120B) plus X-bookmarks when due; **education** = human SI (math, speech patterns, EM styles→teaching) alongside stack self-improvement. **Lane order:** both **ops reliability** and **product** lanes run — **hard priority order**, not either/or — but campaigns ≉ below projects. **Integration testing:** prefer a **testbed** with **gobbledygook fixtures** + Playwright to gate system endpoints before live. **Dashboard UI:** read **`docs/dashboard-ui-architecture.md`** (form families A–E) before edits. **Dashboard Chat:** multi-thread server persistence — **never reset/wipe** thread data on deploy; **new chat** should offer a **campaign picker** (popup/panel) plus optional **entire-campaign context** scope for writing/brainstorming from scratch (characters, markdown artifacts, planning—not fragment-only); mode templates (**Brief RP** / **Workshop** / **Meta ops** / **Agent coding**) — hide model picker on Brief/Workshop; Meta/coding may pick a model (catalog must populate after agent data loads) showing est. **tokens/sec + relative $/token** (skip offline/429-dead); UI must **show the active model**; include **ZenMux** entries in the chat catalog (not "zenmax") e.g. **`zenmux:moonshotai/kimi-k3-free`** when a real free slug exists; **Hub Chat Models free/paid slots must not cross-list** (free models only under Free, never under Paid fallback; prefer free Kimi before Qwen/paid GLM when choosing free main/backup); **explicit model picks stick** — do not silently swap a chosen model (e.g. K3) back to Laguna; **Offload to laptop** (only when checked) creates an `[ops]/load]` user-task **and** a ledger line with thread/mode/campaign context — not on normal Send; **Brief/Workshop** at bottom composer (Enter=send, Shift+Enter=newline); per-message Regenerate/Copy/Branch and **per-message delete** (not only whole-thread Delete); whole-thread **+ Save chat** next to Delete; **Edit** user message → truncate + auto-regen; honest artifact/save confirmations with links (no empty "saved" promises); **+ Promote to task** / **+ Save to campaign** bridge chat → `user-tasks.json` / campaign `notes/`; **free-first** default when no model pick: free models including **Qwen**, **`poolside/laguna-xs-2.1:free`**, and **`tencent/hy3:free`** (hy3 sunsets ~2026-07-21 — prefer Laguna as replacement) → paid **Hermes → GLM → DeepSeek → Venice** only on timeout/empty/429/refusal/API error; prefer **DeepSeek** for **minor** chat work; OpenRouter **`:free`** models can **429 independently of remaining USD budget** (provider free-tier limits ≠ dollar cap) — on free 429 the **backup failover must actually rotate** across the live free pool (not same-model retry thrash); do **not** inbox-ask or pause until the **entire free chain** is exhausted ("that's what the backup is for"); when ops OpenRouter **daily-limit** hits, fall through to **free** models as last resort and **cycle** by **`agents/state/chat-model-usage.json`**; inject read-only **system/task status** into chat prompts (`buildChatSystemStatusBlock`); Chat should show **per-message timestamps** so threads read as a timeline, not an undifferentiated glut.

## Learned Workspace Facts

- **Remote access (PC + laptop):** **Apollo** + **Moonlight** + **Tailscale** (RustDesk fallback); docs in **`docs/remote-pc-setup/`**; scripts in **`scripts/remote-stream-companion/`** (`Connect-RemotePC.ps1`, **`WakeRelay`** → linuxbox WoL, **`Enable-DesktopWake.ps1`**, **`Test-RemoteTailnet.ps1`**). The **500 MB USB `E:` is the only channel to the laptop** (physical plug-in only); the kit was consolidated to the **drive root** (`README FIRST.txt`, `FROM-SCRATCH-LAPTOP.md`, `DESKTOP-PREP-DONE.txt`, `TEST-NOW-BEFORE-TRAVEL.txt`, `scripts\`, **`secrets\id_rsa_potato`**) — the old **`E:\RemotePC-Setup\`** / **`E:\LaptopCursor-Setup\`** subfolders are **SUPERSEDED**; repack from the desktop via **`scripts/pack-laptop-usb.ps1`** (or `pack-laptop-usb.cmd` / `REPACK-USB-FROM-DESKTOP.cmd`); laptop entry **`LAPTOP-INSTRUCTIONS.txt`**, one-click **`SETUP-EVERYTHING-Admin.cmd`**, Apollo pairing **`APOLLO-LOGIN.txt`**, creds from gitignored **`scripts/laptop-usb-kit/secrets-local/`** on repack; Apollo over tailnet **`https://desktop-igqesd4:47990`**. **Apollo** host = **`ApolloService`** (auto-start, survives sleep/reboot), ports **47984/47989/47990/48010**, pair once via web UI **`https://127.0.0.1:47990`** (user `apollo`). Reach the desktop by **MagicDNS name `desktop-igqesd4`** rather than a hardcoded IP — its tailnet IP re-keys on state-wipe/re-login (`100.96.132.49` → `100.118.226.87`); **linuxbox** `100.122.108.94` (home LAN **`192.168.4.59`** on wired **`eth0`** — **`potato-lan`** / Match-exec / prefer-lan default; stale **`192.168.4.23`** wlan and **`192.168.1.191`** dead); desktop home Wi‑Fi recently **`192.168.4.42`**; **laptop** **`abhi-m15-laptop`** (~`100.77.115.62`). At home prefer **LAN** Moonlight host + **`ssh potato-lan`** / **`connect-linuxbox.sh`** / **`prefer-lan-when-home.sh`** when latency/bitrate matter; prefer **SSH `Match exec`** so bare `potato` auto-picks LAN vs Tailscale; Tailscale for away — see **`docs/remote-pc-setup/lan-vs-tailscale-decision.md`**. **Desktop→laptop has no SSH/WinRM** — sync via **git** on the shared clone (`git push`/`pull`); laptop initiates to **potato** via **Cursor Remote SSH** / tailnet. Remote wake-from-sleep: linuxbox **`~/bin/wake-desktop.sh`** broadcasts on **`192.168.7.255`** (home **`192.168.4.0/22`**); desktop **Sleep** not Shutdown. Laptop clone path: **`C:\Users\abhinav\Desktop\MAIN_PROGRAMMING_FILES\agent-dump`**. **PC → linuxbox control:** gitignored **`secrets/linuxbox.env`** (paste Discord etc. once on PC); sync via **`scripts/pc/sync-linuxbox-secrets.sh`**; SSH host alias **`potato`** (`~/.ssh/config`); workspace-open task **`linuxbox: workspace bootstrap`** (`runOn: folderOpen`, skips if no secrets file) — **`docs/pc-linuxbox-workspace.md`**. **Pixi RP:** always-on OpenRouter-only on linuxbox via user systemd **`linuxbox-pixi-rp`** (`:8767`, tree `~/pixi-rp/ObsidianWriterStack`, env `~/.linuxbox-pixi/deckard-local.env`); PC chat-ui often at **`ObsidianWriterStack/PixiApp/chat-ui`**; redeploy **`scripts/pc/deploy-pixi-linuxbox.sh`**; **not** on public abhinavall.net (Tailscale/LAN only; **HTTPS preferred** via Tailscale Serve or local TLS for mobile/PWA — Cloudflare tunnels do not cover Pixi). Optional PC Pixi still binds **`0.0.0.0:8767`** for tailnet but **client/connect URLs use `127.0.0.1`** on Windows (never connect to `0.0.0.0`); OpenRouter **`:free`** Pixi models still hit key **daily reservation/402** caps (not only USD balance)—wire **per-model** turn/prompt profiles rather than one global `CHAT_UI_TURN_MAX_TOKENS`. Laptop/`origin/main` may hold a newer data-driven Pixi design than the PC/potato firefighting fork — diff before large refactors. Bonsai inference stays on PC **`:8000`** for Hermes as an **optional** route — runbook **`docs/pixi-local-bonsai.md`**; when Bonsai is down Hermes continues on OpenRouter/ZenMux. **Satyr-V0.1-4B Q8_0** GGUF on PC (separate port from Bonsai, often `:8001`) is optional local **scene/wiki/person-stub summarizer** — **not** loaded on potato; potato Pixi falls back Satyr-URL→OpenRouter→hydrate. Live Pixi SoT is potato `~/pixi-rp/ObsidianWriterStack` (`linuxbox-pixi-rp` `:8767`); setup notes under **`reports/pixi-summary-testbed/`** when present.
- A low-power Debian-based ARM **linuxbox** is administered over Tailscale (Pi-hole, ebook library, Docker homelab). **Gitea** and **Uptime Kuma** use host ports **13000** and **13001**. **Home LAN uplink:** wired **`eth0`** DHCP **`192.168.4.59/22`**, **100 Mbit** negotiated, ~**70+ Mbit/s** measured over SSH (verified 2026-07-14); **USB Wi‑Fi dongle** (**RTL8188EUS**, **2.4 GHz only**, often congested ch6) remains wireless fallback (~**9 Mbit/s** practical ceiling). The linuxbox should be the Tailscale **exit node** when enabled (advertised on-device; tailnet admin approval required — **`docs/ssh-le-potato-reference.md`** or **`scripts/approve_tailscale_linuxbox_exit_routes.ps1`**); do not treat the Windows desktop as the exit endpoint. Client **Use exit node** can black-hole internet until fixed — recover by disabling exit node, then re-enable with **`--exit-node-allow-lan-access=true`** (**`docs/remote-pc-setup/exit-node-internet-fix.md`**). Also relays **WoL** for the desktop via **`~/bin/wake-desktop.sh`**. Hub **Systems Host resources** exposes live **SoC temp** (`thermal_zone0`/`cpu-thermal`) and **Eth link** (`eth0` speed) via `/api/host-resources` — replaces Power/GPU N/A on this board. The **Amlogic S905X / Le Potato** board boots with a **randomized locally-administered MAC** (e.g. `4e:` prefix) when none is fixed in u-boot, so its DHCP lease/IP can shift across reboots (e.g. `.59`→`.58`) even with the Ethernet link up — identify the box by its **Tailscale node key** (on-disk, independent of MAC/DHCP), not the last-known LAN IP.
- **Linuxbox storage (three tiers):** **System** = SD `mmcblk1` (~60G btrfs `/`) — OS + packages + live services only; **Work USB** = USB LABEL `PERSONAL` (~232G vfat `/media/abhinav/PERSONAL`, alias `/media/abhinav/work`; Hub label **Work USB**, not “PERSONAL USB”) — main working storage (ebooks, `agent-work/`, `projects/`, `exports/`, agent-parseable docs at `agent-work/docs/INDEX.yaml`); **Archive** = HDD LABEL `archive` (~916G ext4 `/mnt/archive`) — cold/slow (logs, state backups, usb-shuttle). Prefer **LABEL + mount** over `/dev/sdX` (letters swap). Agent docs SoT in git under `docs/`; Work USB holds the offline mirror. E: USB shuttle archives → `/mnt/archive/usb-shuttle/`. Bulk agent logs → `/mnt/archive/logs/` only; meta `agents/archive-meta.json` + `agents/state/run-index.jsonl`. Externals in fstab `nofail`.
- **Infranet:** decentralized spare/garage-compute marketplace (eBay analogy) — **not** an LLM-hosting platform (LLM apps are customers). Canonical pitch **`docs/infranet/INFRANET-COMBINED-BRIEF.md`** (+ `.html`/`.pdf` via **`docs/infranet/build-brief.py`**); engineering/PoC **`projects/infranet/`** (`ARCHITECTURE.md`, **`poc/`**). Prefer **how-it-works / how-it-solves-problems** explainers plus try-it examples alongside the brief (open-source survey + theoretical gaps → concrete spikes from `poc/`). Hub **Docs** must index Infranet in the same silo via **`SYSTEM_DOC_SCOPES`**: **`infranet`** → `docs/infranet` (writable), **`infranet-eng`** → `projects/infranet` (read-only) — not campaign-folders-only. **V1** = compute marketplace only (CPU/GPU-runtime metering as **compute tokens**, not LLM tokens; settle via piggybacked agentic-commerce rails **Skyfire/AP2/ACP** — **do not mint** a currency; prefer **VM/micro-VM** isolation over containers as primary). **V2** = identity / age-gating / walled-garden communities. Stubs: design/business md under `docs/infranet/` point at the combined brief. Optional personal spare node: **Android phone on LineageOS** can be connected as extra compute when available (not yet a standing always-on worker).
- **Memory + `mem-constant`:** **MemPalace** = durable archive / source of truth; **Claude Mem** = session working cache; promote on chat/agent/milestone boundaries (optional vault/encryption tier of interest). **`mem-constant`** (`pip install`, **0.4.0**, **34 workflow skills** via `--with-workflow-skills`, **https://github.com/kineticdirt/mem-constant**); workspace dogfoods **`.cursor/skills/`**, **`mem-constant.yaml`**, **`.mem-constant/`** (carryover + **`ops-discipline.md`**), Cursor rule **`.cursor/rules/mem-constant.mdc`**; specs SoT **`docs/memory/`** (incl. **`ops-discipline.md`**: why→prevention on failures); **`_bmad/` deleted 2026-04-26** (self-contained skill templates). **Graphify** (`docs/INTEGRATION-GRAPHIFY.md`): **L1** repo graph; **L5** MemPalace curatorial; evidence L4→L1 only. Home-stack plan does not prioritize large-scale personal cloud file/photo sync (e.g. Nextcloud/Immich); private library via Tailscale/LAN, not a public reverse proxy.
- **Always-on agent (linuxbox):** Hermes **v0.14** gateway (**`hermes-gateway`** user systemd + **linger**); **profiles (FREE-FIRST, rebuilt 2026-07-24 — paid order is DeepSeek then GLM 5.2 as its backup):** **`fast`** = Laguna free → `inclusionai/ling-3.0-flash:free` → DeepSeek last resort; **`think`** = free-first Laguna free → `nvidia/nemotron-3-super-120b-a12b:free` → `inclusionai/ling-3.0-flash:free` (fires every minute, LLM throttled to ~8m via `THINK_INTERVAL_SEC=480`; on free **429** must **rotate** the live free chain — do not inbox/skip while backups remain; paid DeepSeek only under **C8** (`agents/THINK_SECURITY_CHECKS.md`): (1) **entire free pool exhausted** (`THINK_PAID_ON_FREE_EXHAUSTED` / alias `THINK_ALLOW_PAID_LAST_RESORT`) or (2) **verified free failure** — explicit success metric + ≥`THINK_PAID_FREE_FAIL_N` (default 2) free runs that failed harness verify (`THINK_PAID_ON_VERIFIED_FREE_FAIL`); never pay on model self-claim; day-scoped `think-free-429` / `models_429` **ALL-exhausted** must **mid-day re-probe** before scenario-1 paid or Hub “free exhausted” (stale full-day blocklist until UTC midnight is a known false-exhaustion mode); Hub **Active Now** must not show free-429 as **blocked** when failover/paid-last-resort is available); think guardrails include **C8** paid gate; **`code`**/**`meta`** = `cohere/north-mini-code:free` → `nvidia/nemotron-3-ultra-550b-a55b:free` → DeepSeek → **`z-ai/glm-5.2` (DeepSeek's backup only)**. **Do NOT re-add (probe-verified dead — each burns a silent retry hop):** `stepfun/step-3.7-flash` (burned ~$14/day thrashing think), `qwen/qwen3-next-80b-a3b-instruct:free` (delisted 2026-07-24, paid variant only), `zenmux:moonshotai/kimi-k3-free` (**404 invalid_model, never existed**; real `moonshotai/kimi-k3` is PAID $3/M in, $15/M out — do not substitute), `tencent/hy3:free` (sunset 2026-07-21); **`nousresearch/hermes-4-70b`** on watchlist for OR tool-use 404 since 2026-07-20. **ZenMux cannot be a profile primary** (Hermes resolves it as provider `custom`, drops `ZENMUX_API_KEY` → 403) — dashboard/manual `zenmux:<slug>` only. Re-probe ids before editing chains: **`.staging/model-probe/probe_free_models.py`**; **OpenRouter ~$10/day** budget (user-raised from $7; **two keys**: Pixi/RP in `~/.linuxbox-pixi/deckard-local.env`, Hermes/ops in `~/.hermes/.env` — keep separate; policy `agents/model-budget/`, free-first — paid only when free models rate-limit/fail); **ZenMux** = second **selectable** OpenAI-compatible provider alongside OpenRouter (`https://zenmux.ai/api/v1`, key_env **`ZENMUX_API_KEY`**, OpenRouter-style slugs; ZenMux free variants use a **`-free`** slug suffix, **not** `:free`) — select via a **`zenmux:<slug>`** model-id prefix (unprefixed default stays OpenRouter); dashboard **`agents/model-budget/chat-catalog.json`** + Pixi picker expose free K3 as **`zenmux:moonshotai/kimi-k3-free`**; Hermes v0.14+ registers it via the `providers:` keyed map in each profile `config.yaml`, dashboard maps the prefix to `hermes chat --provider zenmux -m <slug>`; enable additively/idempotently on the box with **`scripts/linuxbox/add-zenmux-provider.sh`** (do **NOT** full-rerun `install-hermes-profiles.sh` — it reverts think's PC-Bonsai routing); soft **$5/day** target in `agents/model-budget/config.json` (`providers.zenmux.daily_usd_target`, override `ZENMUX_DAILY_USD`; soft policy, not hard-enforced in code); RP pods **`hunter-reckoning`**, **`tropic-gooner`**, **`spacequest`**, **`nyc-mafia-dnd`** (`agents/agent-pods.manifest.json`) — **Tropic Gooner** and **Hunter: The Reckoning** share **`campaigns/tropic-gooner/`** (same chronicle) but **Hunter** is a separate pod/layer + Discord live bot; Bot automation/scripts must read **`DISCORD_BOT_TOKEN`** (canonical; live on hunter profile + tropic `.env`; pointer comment in `~/.hermes/.env` — no active BOT_TOKEN there while ops+hunter both run); **`DISCORD_TOKEN`** may remain as a compatibility alias where already wired — prefer **`DISCORD_BOT_TOKEN`** for new code; **`DISCORD_ALLOWED_USERS`** is a **Hermes allowlist**, not a Discord API field; live Discord gateway is **`hermes-gateway-hunter-reckoning`** (ops `hermes-gateway` must not claim the same bot token); installers **`scripts/linuxbox/install-hermes-profiles.sh`** + **`install-hermes-agent-crons.sh`**; Hermes **safe git allowlist** via **`patch-hermes-approvals-git-safe.py`** (`git status|pull|fetch|log|diff|rev-parse`; deny force-push/hard-reset); **think must not block/inbox on `git pull`** (private repo → PC git-bundle sync only); **`FIRECRAWL_API_KEY`** in **`~/.hermes/.env`** only; prefer **Firecrawl cloud browser** over local Chromium on **~2 GB RAM** (2 GB **`/var/swap`** on BTRFS: **`chattr +C`** then **`dd`**). **`agent-cycle-think`** crontab **1m** (sync deterministic each minute via **`agent-cycle-sync.sh`**; LLM interval **`THINK_INTERVAL_SEC=480`**; setup inject **`think-setup-context.py`**). **Fast lane removed** 2026-08-01. Hermes cron jobs paused in favor of crontab ticks; **think guardrails** are ordered **security checks** (`agents/THINK_SECURITY_CHECKS.md` **C0–C8**), not rigid bans that strand work — only **C0** is hard deny; **C1** shell via logged **`agents/state/shell-access-forms/`** + `--yolo`; **C4** must set user-task `status=done|blocked` (never leave `open` after saying blocked); **C6** UI → Playwright chat/dashboard smoke; **C7** soft finish ~12 tool steps / hard **`--max-turns` 18** default, **24** for `[ops]`/UI, or **28** when escalated (`THINK_MAX_TURNS_*`); think reports under `/mnt/archive/logs/think-reports/` + `reports/think-ticks/LATEST.md`; **`agent-pod-scheduler.sh`** must not let **fast starve** think/campaign pods when any non-fast pod is due; **swarm MoE:** **`swarm-dispatch.sh`** + **`agents/swarm-experts.json`** + **`agents/swarm-queue.json`** (60s timer, fast tick); human Q&A via **`agents/state/human-inbox.json`** (gitignored runtime; **`agents/human-inbox.json`** must be a **symlink** to state — a real empty file breaks pods; seeds **`agents/inbox-seeds.json`** re-merge by **stable id** until answered — enrich context, never re-ask answered); Hermes "down" is often **D-state hang** on bloated **`~/.hermes/state.db`** (not crash) — **`hermes-gateway-watchdog`** + Hub **HUNG** (`nousagent-health`); archive bloated DBs to **`/mnt/archive/logs/`**; **Meta-Harness** pod traces **`agents/meta-harness/runs/<pod>/`** + **`scripts/meta-harness/score_tick.py`** / **`query_runs.py`**; alternates **SpaceQuest** (`campaigns/spacequest/`, no VTT/maps) and **NYC Mafia × D&D** worldbuilding via **`agents/CURRENT_TASK.md`** (portfolio/blog when USB mounted). Other crons: **`intel-feed-health`** `0 7 * * *` + **`situation-rss`** `0 8 * * *` + **`situation-hermes`** `0 9 * * *`; **`site-abhinavall-ping`** / **`site-abhinavall-review`**; **daily maintenance** lane **`agents/DAILY_MAINTENANCE_TASK.md`** when **`agents/maintenance-progress.md`** has open items. **Intel/News dashboard:** **18 RSS** feeds (OpenAI blog removed 2026-07). **Compiled intent:** `agents/intent/agent-loops.json` + post-run **`verify_agent_intent.py`** (`INTENT_OK`/`INTENT_FAIL`; guide **`agents/AGENT_LOOPS_INTENT.md`**). Runbooks: **`docs/agents/linuxbox-hermes-owl-alpha.md`**, **`situation-monitor-lane.md`**, **`website-abhinavall-lane.md`**. **PC:** WSL Hermes + **`deckard-local.env`**; **`scripts/hermes/continuity_run.sh`** (situation monitor + gitignored **`hermes/state.json`**); daily **`scripts/situation_monitor/`** → **`reports/situation-monitor/`**. **`CLAUDE.md`** guides the linuxbox agent; upgrades gated by **`safe-update-check.sh`** (auto-upgrade only on **SAFE** verdict). **deepsec** optional: **`agents/deepsec-config.json`** (`enabled: false` default) — linuxbox **`scan` only** via **`scripts/linuxbox/deepsec-scan.sh`**; **`process`** on PC only.
- **Personal site:** **https://abhinavall.net/** (live portfolio; GitHub **@kineticdirt**). **Live production** on linuxbox: **`personal_portfolio`** Node app (`api/server.js`) on **:3000** — not the PC static bundle or `/home/abhinav/www` placeholder. **CF tunnels (linuxbox only, split policy):** **`cloudflared-abhinavall`** (`abhinavall.net` → **`abhinavall-origin-8780`** / **`tunnel-origin-proxy.js`**: **`/Intel*`** → **:8790** public, **`/Linuxbox*`** → **:8790** admin, else **:3000**) + **`cloudflared-tableslop`** (`WOD_HTR_LinBox_TABLESLOP` / **`map.tableslop.org`** → **:8765**; **`campaigns.tableslop.org`** → **:8768** `linuxbox-campaigns-avail`); **tableslop** = RP frontend + campaign availability, **abhinavall/linuxbox** = backend — minimal tunnel overlap; **never** run deprecated **`install-cloudflared-tunnel.sh`** (connector thrashing); canonical **`docs/cloudflare-tunnels-linuxbox.md`** (+ **`docs/cloudflare-tunnel-abhinavall.md`** for :8780 detail); after **`cloudflared`** apt upgrade restart **both** named units (currently **2026.6.1**). **Agent dashboards** on **:8790** (`linuxbox-status`): **admin** at **`/Linuxbox/`** — two-layer auth (Cloudflare Access Google SSO/OTP + multi-account HTTP Basic via **`~/.linuxbox-dashboard/.env`** / extra-accounts map — admin-role + viewer-role named users; optional **2-day temp admin** accounts in runtime state; loopback exempt **only when no edge headers**); **Hub `/Linuxbox/` static assets** (e.g. **`docs-wiki.js`**) must use the absolute Hub path **`/Linuxbox/docs-wiki.js`** (not site-root `/docs-wiki.js`, which hits portfolio `:3000` and **404s**; not bare relative `./docs-wiki.js`, which resolves to `/docs-wiki.js` when the Hub URL lacks a trailing slash — e.g. `/Linuxbox?tab=docs` — and breaks Docs with “docs-wiki.js missing”); **Hub timestamps** display **`America/New_York`** (EST/EDT), not UTC; Chat threads should surface **message timestamps** for a readable timeline; **public viewer POC** at **`https://abhinavall.net/Intel/`** (no Access, no password; news/stocks/public docs only); **Systems** tab probes tunnel **`public_url`** (warn = loopback OK, Cloudflare edge down); **Chat threads** persist server-side at **`agents/state/chat-threads/`** (gitignored runtime; survive pull/deploy; explicit Delete only — not in git backup). Before dashboard UI edits read **`docs/dashboard-ui-architecture.md`**; after edits run **`bash scripts/linuxbox/run-dashboard-ui-smoke.sh`** (`.staging/portfolio-redesign/_screenshots/dashboard-ui-smoke.mjs`) and prefer smokes against **`/Linuxbox/`** path-prefix when testing Docs scripts. CF API token in **`~/.cloudflare/access-setup.env`** — **`docs/cloudflare-access-linuxbox.md`**. **Redesign/staging:** PC **`sites/abhinavall.net/`** and **`.staging/portfolio-redesign/`** v8 (preview linuxbox **`:8765`** / PC **`:8766`**); **v8-brutalist-map** direction; Playwright smoke **`_screenshots/portfolio-smoke.mjs`**; **AI blog lane** preview-only — no auto-deploy to production. **tableslop · Isla Primavera:** WoD campaign map platform — **tableslop** = platform; HUD/display name **Isla Primavera** only (no separate "Primavera" label); dual-app roadmap **`docs/plans/tableslop-dual-app-roadmap-2026-08-01.md`** (campaigns vs map; Setup→Beta; deterministic pregen). Task boards **`projects/tableslop/manifest.json`** + per-region **`regions.json`** (R1–R14; status lanes: planning/writing/testing/blocked/done/deferred). **`scripts/linuxbox/tableslop-server.js`** + systemd **`linuxbox-tableslop`** on **`:8765`**; public **`https://map.tableslop.org/`** — **`docs/tableslop-linuxbox.md`**. **Campaign availability:** **`scripts/linuxbox/campaigns-availability-server.js`** + **`linuxbox-campaigns-avail`** on **`:8768`**; canonical **`https://campaigns.tableslop.org/`** (live on `WOD_HTR_LinBox_TABLESLOP` → `:8768`); optional interim **`https://map.tableslop.org/camp/`** (tableslop-server proxies `/camp` → `:8768`). **80s vaporwave** HUD; pan/zoom camera + R1–R14 legend; overworld `campaigns/tropic-gooner/map/map.json`. **Character registry:** `characters-registry.json` + **`tools/sync_character_registry.py --write`** after ingest; use **only** `tools/ingest_discord_sheets.py` (legacy author-slug ingest creates duplicate `characters/discord/*.md`). **Client-first** pilot profile in **`localStorage`** (`projects/tableslop/client-first-profile-plan.md`) plus **Discord OAuth** on the map server with SQLite-backed users/tokens and roles **`owner`/`admin`/`user`** (public view; login to mutate borders/coords). **Map UX:** pins at baked **label** positions (names↔coords one SoT; **`vibes.png`** place spellings win); **SVG selectable area overlays**; **edit mode** drag+save coords to `map.json`. **Local design:** `projects/tableslop/design/` + `scripts/tableslop/serve-design-preview.sh` (**`:8767`**). Hybrid map pipeline **`scripts/tableslop/`** (`npm run map:hybrid` — terrain Real-ESRGAN PyTorch/Lanczos, **`label_layer: ui`** in viewer not baked into PNG; tile pyramid picks correct zoom level; **reject AI upscale for text** — melts labels; map binaries gitignored, deploy **`scripts/linuxbox/push-tableslop-map.sh`**). **Tunnel origin:** **`http://127.0.0.1:8765`** not `localhost` (IPv6 `[::1]` refused when app is IPv4-only).
- In-flight multi-phase migration plans live as untracked **`*-plan.md`** files at workspace root; promote into **`docs/`** only after sign-off; **`AI_GROUPCHAT.md`** carries a **`[PC] Intent`** line for cross-machine coordination.
- **Cursor CLI on linuxbox (gated lane, potato-only):** `agent`/`cursor-agent` v**2026.06.26-7079533** at **`~/.local/share/cursor-agent/`** (symlink **`~/.local/bin/agent`**, already on PATH). Auth via **`CURSOR_API_KEY`** in **`~/.cursor-agent.env`** (chmod 600; paste key from **Cursor Dashboard → API Keys**) or `agent login`. **Never** hang Cursor on think default chain or high-frequency crons. Hub Chat / dispatch: stick to **`cursor:auto` only** (no paid Cursor model picks on the box); runs **`scripts/linuxbox/cursor-agent-run.sh`** on potato with repo skills (`docs/plans/hermes-cursor-agent-lane-2026-07-29.md`) — that runner **must exist** on potato (missing → silent “dispatch”/idle; tick must **fail-loud**); Cursor tick logs under **`/mnt/archive/logs/cursor-agent/`**; after git-bundle/deploy, re-ensure potato **`scripts/linuxbox/*.sh`** stay **`+x`** (lost mode bits → watchdog **`203/EXEC`** / Permission denied). Prefer **Cursor Auto as semi-free** over paid DeepSeek for ops/Fix-this when work is Cursor-eligible (routing **free → cursor:auto → paid**; think must not burn paid or hit 28/28 and idle while Cursor sits unused on open tasks). Keep a **~30m free-model readiness** cache (probe **free OpenRouter**, not Cursor); when free is known-down, **Agent coding** / Tasks “other current” should **default to Cursor** instead of thrashing free endpoints—Cursor also helps for **screenshot/image ingest** and **portfolio** work. Prefer **Cursor Python SDK** when wiring Cursor as an on-box AI engine ([docs](https://cursor.com/docs/sdk/python)). **Potato-only** — do **not** change the PC/local Cursor install. May run as a **parallel lane** (e.g. campaign worldbuilding drafts / map digitizing) while Hermes think covers other linuxbox tasks — **Agent1 Hermes (OpenRouter+ZenMux) ∥ Agent2 Cursor Auto** (simultaneous; not either/or): Hub should surface the Cursor Auto / SDK lane **side-by-side** with Hermes (use empty Hub space—not sequential-only) and show **similar information** on both lanes (parity—not a thin Cursor stub vs rich Hermes panel); dual/separate workers so Agent-coding Cursor does not block Hermes Hub chat; status via `bash scripts/linuxbox/cursor-lane-status.sh`. **Inject run/lane goals** into prompts when ticks/jobs start so the model can self-check whether actions match expected outcomes. **Hermes** still owns the free always-on OpenRouter lane. For **PC-offline / away** work prefer **Cloud Agents** at **`cursor.com/agents`** on the **Linuxbox** repo (Phase 2 / GitHub connect still needed) over leaving the desktop IDE up or cronning CLI — see **`docs/plans/cursor-auto-away-from-pc-2026-07-29.md`**. Per the SAFE-gate, invoke headless as **`agent -p --force --disable-auto-update "…"`** and upgrade only via **`agent update`** after a supply-chain check. Inherits this repo's `.cursor/rules` + `AGENTS.md` + MCP.
- **Repo split (Linuxbox vs mem-constant):** homelab infra → private **`github.com/kineticdirt/Linuxbox`** branch **`main`** (linuxbox `~/agent-dump`, **cursor.com/agents** push here); **`mem-constant`** = versioned **PyPI memory-tool** only (`release/v*`) — **not** homelab/campaign/inbox storage. PC working tree may still origin **`mem-constant`** historically; publish homelab via **`scripts/pc/publish-linuxbox-repo.sh`**. **Git sync to box:** private repo — no HTTPS pull on potato — PC **`scripts/pc/push-linuxbox-git-bundle.sh`** → box **`apply-git-bundle.sh`** (fast tick; **`--hard`** for code only — runtime paths preserved via manifest); the bundle applies the **`linuxbox/main`** ref — if PC `master` has diverged, publish/force-fetch first (`scripts/pc/publish-linuxbox-repo.sh`; last-resort force-fetch fallback lives in `apply-git-bundle.sh`) or the box silently stays behind; SCP **`push-linuxbox.sh`** for binaries/urgent (tarballs filtered through **`protected-paths.py`**). **Runtime vs code contract** (`docs/runtime-state-protection.md`): **`campaigns/*/characters-registry.json`**, **`agents/state/**`**, **`agents/user-tasks.json`**, portraits, map edit saves = **potato-owned** (gitignored on `main`, manifest-preserved, timestamped backups under **`/mnt/archive/state-backups/`**); dashboard HTML/JS = **git-owned** (commit to `main`, not SCP-only overlays). **Never push PC campaign `progress.md` boards over potato** when potato is already all-done / ahead (esp. `tropic-gooner`) — that reopens think thrash. Post-deploy **`verify-runtime-state.sh`** + marker verify (`active-work` / `chat-threads-toggle` / Running now); on VERIFY FAIL open a stable-id Hub Inbox alert (never re-ask an already-**answered** `runtime-verify-*`) and fail loud—do not proceed as if healthy; treat **registry version/watermark** as potato-owned truth (never clobber a newer potato `characters-registry.json` with an older PC/deploy copy). **Never commit secrets**; mem-constant public history may still expose older infra topology.
- **Pixi RP source repos / reconciliation:** Pixi RP / **ObsidianWriterStack** dev tree (PC path `C:\Users\abhinav\Desktop\MAIN_PROGRAMMING_FILES\ObsidianWriterStack`) is versioned on GitHub **`RP_TESTBED`** (`git@github.com:kineticdirt/RP_TESTBED.git`); **laptop `origin/main` is the canonical data-driven architecture line** — the PC "firefighting fork" **ports its fixes onto laptop main** (not the reverse; laptop architecture wins, PC supplies fixes like OpenRouter-only/Laguna/402-retry/permanence/hygiene/dead-code tooling). **Never force-push, never overwrite canonical main with the PC dirty tree, never delete `~/pixi-rp/ObsidianWriterStack`.** Standing up **Gitea on linuxbox `:13000`** as the canonical RP repo host (seed `main` from GitHub `origin/main`, mirror named branches e.g. `pc/dead-code-tooling`, `pc/wip-sheet-permanence`). When the user says **git-tea**/Gitea for commit-and-push, check remotes and prefer a configured Gitea remote on **`:13000`** — do not assume GitHub-only without checking. Reconciliation/diff plans live under **`docs/plans/`** (`branch-reconciliation-plan.md`, `pc-vs-laptop-feature-diff.md`, `gitea-rp-canonical.md`) — this RP repo is distinct from the `Linuxbox` / agent-dump repo.
- **Windows/git caveats (PC):** PowerShell `.ps1` scripts are blocked by the default execution policy — run with **`-ExecutionPolicy Bypass`** per-command (don't change the system policy) or use the bundled **`.cmd`** double-click launchers; do **not** run **`tailscale login`** from agent/Cursor terminals (logout loops — use Tailscale GUI); USB kit **`.ps1`** must stay **ASCII-only** for laptop PowerShell 5.1; a stray Windows-reserved file named **`nul`** in the repo root makes **`git add -A`** fail (exit 128), so stage paths selectively instead; mingw **`curl` 8.8.0** on this PC errors (**code 43**) when **`-w`** is combined with **https** — use **`-i`**/response headers instead; PC fans: prefer **FanControl** — do not let Windows reclaim/disable it (user does not want OS thermal takeover fighting FanControl); **never shut down / power off the Windows desktop** unless the user gives an **explicit standalone** shutdown request (do not infer from ambiguous “when finished” wording).
