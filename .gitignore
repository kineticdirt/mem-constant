# Private keys — never commit
id_rsa_potato
*.pem

# Machine-specific remote streaming config (Tailscale IPs, MACs)
scripts/remote-stream-companion/config.psd1

# Python packaging / test
*.egg-info/
dist/
build/
.pytest_cache/
__pycache__/
*.pyc
