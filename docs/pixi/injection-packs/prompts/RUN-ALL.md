# Ready-to-paste order (all four)

For each of 4 notebooks, paste in this order:

1. Entire contents of `00-SETUP.md`
2. Entire contents of `00b-FAILURE-MODES.md`
3. The pack file (`01` … `04`)
4. Optional: paste `CONTRACT.md` JSON schema once if NotebookLM has no other copy

## Source hints (failure-oriented)

| Notebook | Prefer sources that teach… |
|----------|----------------------------|
| kit_gear | garment construction, wear positions, holster/sling/stow, armor coverage |
| world_storage | inventory schemas, nested places, “if it isn’t written it isn’t real” GMing |
| system_dynamics | Blades-style clocks, fronts, attrition, off-screen faction ticks |
| mechanics | ONE system only (infection *or* hard magic *or* hard sci) with costs/limits |

## After each run

Save under `returns/` and message: `ingest kit_gear return` (etc.). Agent dry-runs
all four before applying to potato.
