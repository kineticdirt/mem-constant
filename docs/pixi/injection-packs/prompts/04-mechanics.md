# Prompt — pack `mechanics`

Paste after Setup (+ optional failure-modes brief). Attach **one coherent system** of sources (infection-only *or* hard-magic-only — do not mix contradicting cosmologies).

---

Produce JSON for **`pack_id`: `mechanics`**.

## Failures this pack must counter

1. **Rule upgrade** — powers/infection/tech quietly get stronger mid-scene
2. **Free power** — effects with no cost, limit, or visible tell
3. **Accuracy / consistency** — contradicting an established rule next beat
4. **Omniscience** — characters narrate true mechanisms they have not observed
5. **Forgotten mechanical state** — burned charge / contracted infection / fried circuit only in prose

## Split output

- `lines` = **generic** discipline (any system)
- `scenario_notes_template` = **this notebook’s** setting rules (≤6 bullets) for `injection_pack_notes.mechanics`

## Baseline (generic)

```
- Rules hold across beats; consistency > spectacle.
- Cost, limit, visible tell; no free power.
- Established mechanics are canon; limits push back.
- Unknowns stay unknown unless earned in-fiction.
- Record mechanical state in WORLD_DELTA.
- Failure is legitimate.
```

## Deliver

`failure_modes_addressed` must include rule-upgrade + free-power + forgotten state. Prefer `mode: "diff"` for generic `lines`.
