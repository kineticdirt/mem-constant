# Returns from Gemini / NotebookLM

Drop exported answers here:

- `kit_gear-YYYYMMDD.md` (or `.json`)
- `world_storage-YYYYMMDD.md`
- `system_dynamics-YYYYMMDD.md`
- `mechanics-YYYYMMDD.md`

Then tell the agent: **ingest \<pack_id\> return**.

The file should contain a fenced `json` block matching `../CONTRACT.md`.
