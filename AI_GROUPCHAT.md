- **2026-07-26T02:14Z** — [PC] **Result:** Hub Map+Campaigns link-out live (`db-20260726-map-linkout-r1`). Root: potato Hub iframes pointed at `http://127.0.0.1:8765/` (broken from remote/HTTPS). Replaced Map tab + Camp map embed with CTAs → https://map.tableslop.org/ (+ #cast). SpaceQuest `archived:true` (picker demoted; no dead map). tableslop already healthy: public root/map-image/tile **200** (~10.7MB PNG). Open **https://map.tableslop.org/** for the real map. Hard-refresh Hub. No commit.
- **2026-07-26T02:10Z** — [PC] **Intent:** Hub Map + Campaigns broken map images. Canonical interactive map = https://map.tableslop.org/ (:8765). Convert Hub Map tab + Campaigns map embeds to link-out CTAs; stop presenting archived SpaceQuest as live map campaign; verify/push tableslop map assets; deploy dash. Holder: hub-map-tableslop-linkout. No commit.
- **2026-07-26T02:01Z** — [PC] **Result:** Cursor update staged. Was **3.12.30** (Program Files system install). Latest **3.13.10** downloaded to `Downloads/cursor-update-2026-07-25/CursorSetup-x64-3.13.10.exe` (191M); elevated installer launched. User must approve UAC + close Cursor to finish. Stale user install also at `%LOCALAPPDATA%\Programs\cursor` — ignore/remove later if dual-install confuses updates. Holder: cursor-win-update.
- **2026-07-26T02:00Z** — [PC] **Intent:** Cursor IDE update on Windows — auto-update broken; manual system-setup 3.12.30 → 3.13.10. Download + run CursorSetup-x64-3.13.10.exe. Holder: cursor-win-update.
- **2026-07-26T01:50Z** — [PC] **Intent:** Pixi tracking/inject/agency. Diagnose why clothing/inventory/weapons not updating as scenes advance; locate injection-pack path and wire packs into Send contents; stop model answering player questions with NPC questions — force decisions. Audit potato :8767 first; plan before large code. No wipe.
- **2026-07-25T21:56Z** — [PC] **Result:** Hub false-blocked on free-429 despite paid last-resort. Root: `/api/agent` `work_pipeline.free_429_blocked` was true whenever all free ids were in `think-free-429.json` — ignored `paid_last_resort=true`. Hub Active Now idle line always prefixed `blocked: OpenRouter free 429`. Fix: blocked only when exhausted AND not paid; expose `free_429_exhausted`/`paid_last_resort`/`paid_last_resort_model`; UI copy `free exhausted → paid deepseek/deepseek-v4-flash`. dash `db-20260725-activenow-paid-r1`. Verify: API free_429_blocked=false; live tick hermes `-m deepseek/deepseek-v4-flash` → focus done `PAID last-resort deepseek/deepseek-v4-flash` (tableslop ts-c, 21:54–21:56Z). Inbox already answered. No commit.
- **2026-07-25T21:54Z** — [PC] **Intent:** Hub Active Now still shows blocked on think-free-429 despite paid_last_resort=true. Fix server free_429_blocked + UI copy (free exhausted → paid DeepSeek); verify think tick. Holder: active-now-false-block. No commit.
- **2026-07-25T17:02Z** — [PC] **Result:** Think unblocked via **paid last-resort**. `agent-cycle-think-tick.sh`: free rotate/fast-probe first; when full `think-free-swap` 429 for UTC day → `hermes -m deepseek/deepseek-v4-flash` (THINK_ALLOW_PAID_LAST_RESORT=1); no skip-Hermes. Inbox `think-free-429-20260725` answered (paid until free reset); state `paid_last_resort=true`. Swap demo on potato: Laguna/Ling/Nemotron all HTTP 429 Remaining=0; DeepSeek flash OK; Hermes think `-m deepseek` replied OK (~21s). Weekly check: `probe-free-models-weekly` cron **Sun 07:15** (new, script `probe-free-models-weekly.sh` → `reports/model-probe/` + swap `last_probe`); daily `hermes-model-health` **06:45** already live (`hermes-model-failover.sh`, report today present). Deployed `~/bin` + scripts. No commit.
- **2026-07-25T16:57Z** — [PC] **Intent:** Unblock think — free rotate first, then temporary paid DeepSeek last-resort when full free swap 429 for UTC day; clear skip-Hermes; swap demo on potato; confirm weekly/daily free-model probe. Holder: think-paid-last-resort. No commit.
- **2026-07-25T16:57Z** — [PC] **Intent:** Systems tab — add stable short `ref` per service card (hub/tableslop/hermes/pixi/…); show badge + DETAILS; bump DASH_BUILD; deploy potato; verify. Holder: systems-ref-names. No commit.
- **2026-07-25T16:59Z** — [PC] **Result:** Systems `ref` short names live (`db-20260725-sysref-r1`). Badge on cards + DETAILS Ref/Id. Map: tunnel-abhinavall, tunnel-tableslop, portfolio, hub←dashboard, tableslop←tableslop-app, pixi (new :8767), hermes←hermes-ops, hunter←hermes-hunter, scheduler←pod-scheduler. Potato /api/systems refs OK; :8790 200; pixi icon 200. hunter health=down (known). No commit.
- **2026-07-25T16:38Z** — [PC] **Result:** Think free swap expanded to **8** live-catalog ids (SoT `agents/model-budget/think-free-swap.json`): Laguna-XS → Ling → Nemotron-super → North → Nemotron-ultra → Laguna-S → Gemma-4-31B → gpt-oss-20b. Probe 16:35Z: **all 8** HTTP 429 `free-models-per-day-high-balance` (shared OpenRouter free daily pool Remaining=0). Failover: Hermes same-provider fallbacks do **not** rotate ids (3× same `-m`); think tick outer `hermes -m` rotate + fast-probe on shared-daily. Inbox/skip-Hermes **only** when full list exhausted — rewritten `think-free-429-20260725` (no re-ask). Wired: `~/bin` tick, think profile 7 fallbacks, chat-catalog v4, config.routing.free_models, hermes-model-registry, research-studies-models. Hub `/api/model-budget` free=8. Skip Hermes correct until ~00:00Z. No commit.
- **2026-07-25T16:35Z** — [PC] **Intent:** Expand think free swap beyond 3 — probe wide OpenRouter :free pool; wire rotate + catalog/Hub; inbox only when whole live free set exhausted. Holder: think-free-wide-pool.
- **2026-07-25T16:34Z** — [PC] **Intent:** User pushback on think free 429 — backups exist; do not inbox/skip on primary-only 429. Prove Laguna→Nemotron→Ling rotate; clear false full-chain seed; soften inbox think-free-429-20260725. Holder: think-free-429-failover.
- **2026-07-25T16:11Z** — [PC] **Result:** Temp viewer link generator live on potato (:8790). Systems tab Temp viewer links; admin POST/GET /api/auth/temp-accounts + revoke; redeem GET /api/auth/temp-redeem?token= sets HttpOnly lbx_token cookie (viewer <=2d). Runtime agents/state/dashboard-temp-accounts.json (prunes expired). dash-build db-20260725-tempview-r1. Verify: create->Basic+cookie session viewer; news 200; systems 401; forced expiry+revoke->401; list has no passwords. No secrets in ledger. No commit.
- **2026-07-25T16:08Z** — [PC] **Intent:** Temp viewer link generator for Linuxbox dashboard — admin API+Systems UI; runtime `agents/state/dashboard-temp-accounts.json` (2-day expiry); redeem sets lbx_token cookie; no passwords in ledger. Deploy potato + verify. No commit.
- **2026-07-25T16:03Z** — [PC] **Result:** Dashboard Basic auth multi-account on potato. Server loads DASHBOARD_EXTRA_ACCOUNTS; accounts admin+HeadUser (admin), guest+viewer (viewer); admin/viewer passwords rotated; .env 600 + bak.pre-auth-rotate; linuxbox-status restarted; journal lists 4 Basic users (no secrets). Verify (X-Forwarded-For): wrong/no-auth 401; all 4 session roles OK; guest denied /api/systems; admin+HeadUser systems 200. Docs/example updated (no plaintext passwords). Credentials only in human chat reply — not ledger/git. No commit.
- **2026-07-25T16:00Z** — [PC] **Intent:** Dashboard Basic auth — rotate admin; add named accounts HeadUser (admin) + guest (viewer); keep viewer; extend server multi-account from box .env only; no plaintext passwords in ledger/git. Deploy potato + restart linuxbox-status; verify roles. No commit.
- **2026-07-25T04:44Z** — [PC] **Result:** EM education architecture done. Aborted prior `shutdown -t 1200`; rescheduled `shutdown -s -t 60` (abort+schedule exit 0; ETA ~00:45:16 Eastern Daylight Time).
- **2026-07-25T04:43Z** — [PC] **Intent/Result:** `shutdown -s -t 1200` scheduled (~20m, ETA ~01:03 EDT / ~04:03Z). Event 1074 confirmed 00:43:34 local. Remaining Cursor/EM-education wrap should finish inside window; leaving 1200s timer (not shortened — not clearly idle).
- **2026-07-25T04:43Z** — [PC] **Result:** Continuous-lane arch shipped. Doc `docs/agents/continuous-lanes.md` (campaign≡project RR → education → research → IDLE). Education board seeded 6 EM boxes `si-edu-em-{servant,situational,okrs,feedback,psych-safety,teach-back}` (+6 core; 12 open `[ ]` total). Wired CURRENT_TASK/CLAUDE/SELF_IMPROVEMENT_TASK + think FREE_NOTE. Potato verified: progress has all 6 EM open, doc present, `~/bin` Lane=education. Research board untouched. No commit.
- **2026-07-25T04:45Z** — [PC] **Result:** Research continuous lane wired into four-lane model (**campaign · project · research · education**). Spec `agents/RESEARCH_STUDIES_TASK.md` + board `agents/research-studies-progress.md` (10 open `rs-bench-*`, free-model benchmark → `reports/research/free-model-benchmark-YYYY-MM-DD.md`); models `agents/research-studies-models.json` free-only default **Nemotron-super** + Laguna/Ultra/Ling/North. Priority: after ops+project/campaign (+meta), **education then research**, before IDLE. has-work/think-tick/CLAUDE/CURRENT_TASK aligned; Education task header renamed (progress board **not** clobbered — still 6 `si-*`). Probe path: `research-studies-probe.sh` (max_tokens 1600). Potato probes: Laguna reasoning ok=True but content empty (finish=length, 800 reasoning_tokens @ old cap); Nemotron-super reasoning **full** ~6.3s. has-work still `WORK: open user-tasks` (research waits until quiet). Deployed repo+`~/bin`, CRLF stripped. No commit.
- **2026-07-25T04:41Z** — [PC] **Intent:** Continuous-lane architecture (campaign ≡ project RR → education then research idle) + seed EM-styles education boxes on SI board; doc `docs/agents/continuous-lanes.md`; wire CURRENT_TASK/CLAUDE/think notes; deploy potato open `[ ]` for next quiet think. Sibling owns research — no wipe. No commit.
- **2026-07-25T04:42Z** — [PC] **Intent:** Align research/studies with four continuous lanes (**campaign · project · research · education**). Research = studies/benchmarks board; Education = SI (sibling owns SI board seed — do not clobber). Finish probe max_tokens redeploy + ledger Result. No commit.
- **2026-07-25T04:38Z** — [PC] **Intent:** Research/studies idle lane — create `agents/RESEARCH_STUDIES_TASK.md` + `agents/research-studies-progress.md` (first project: free-model benchmark); wire has-work/think **after SI, before IDLE**; free-only model list (Laguna / Nemotron-super default / Nemotron-ultra / Ling / North-mini); deploy potato; optional 1-model probe. Distinct from human SI + X bookmarks. No commit.
- **2026-07-25T04:34Z** — [PC] **Result:** Dashboard Chat timeline `db-20260725-chattime-r1`. Messages already store `at` (epoch ms) on write; UI now shows muted ET bubble ts (`Jul 24, 11:42 PM`) + day separators; hover = full `fmtEt`. Legacy threads with no per-msg `at` get thread-level day label only (no invented precision). Potato: scp server+index, restart, :8790 200, dash_build match; sample threads 294 msgs with `at`. No commit.
- **2026-07-25T04:32Z** — [PC] **Intent:** Dashboard Chat timeline — inject per-message timestamps (America/New_York) on bubbles; stamp `ts` on write; legacy backfill soft (mtime/thread updated_at or unknown); bump DASH_BUILD; deploy potato. No commit.
- **2026-07-25T04:30Z** — [PC] **Result:** News/Intel quality pass. situation-rss feeds **18→24** (+6: Quanta, BBC Science, MIT TR AI, Foreign Policy, Bellingcat, Just Security); none removed (all prior healthy). intel-trackers social **11→17**. Digest: tag-first categories + science/policy/osint/tech, per-cat cap 8, richer snapshot. Intel UI cap 12→20 so new feeds appear. Potato: feed-health all_pass; digest 24/24; `/api/intel` social_len=17. Doc `reports/news/SOURCES-2026-07-25.md`. No commit.
- **2026-07-25T04:30Z** - [PC] **Result:** Portfolio continuous lane reactivated. **Why inactive:** (1) think crontab paused ~22:19-03:25Z for Step burn; (2) `portfolio-progress.md` only seeded today - prior path was USB overnight + ping/review only; (3) RR had not yet rotated onto portfolio (was tableslop/pixi). Site never down: `abhinav-portfolio` active, `:3000`/`https://abhinavall.net/`/`8780` **200**, ping LATEST-CHECK 200. **Activated:** open `[ ]` pf-b/c/d on board (pf-a done); has-work+think CONTINUOUS_LANES already include portfolio; RR `last=portfolio-progress.md` @04:27Z. **Concrete:** v8 timeline depth - index rail, edge fades, wheel-to-x + arrow keys; Playwright smoke PASS (`PREVIEW_URL=http://127.0.0.1:8766/v8-brutalist-map/`). Staging synced potato `.staging/portfolio-redesign/v8-brutalist-map/`. Preview-only; no live :3000 edit. No commit.
- **2026-07-25T04:26Z** — [PC] **Intent:** News/Intel quality pass — inventory feeds+pipeline; fix/remove dead; add 4–8 high-signal sources (science/AI/policy/security/OSINT); improve situation-rss digest structure; deploy potato; verify feed-health/dry digest; doc under `reports/news/`. Free-first. No commit.
- **2026-07-25T04:26Z** — [PC] **Intent:** Diagnose why abhinavall portfolio lane inactive; reactivate continuous board (`agents/portfolio-progress.md` open `[ ]`) + think RR reachability; one concrete staging improvement (no live :3000 edit unless broken); verify potato `:3000` / site ping. Free-first. No commit.
- **2026-07-25T04:10Z** — [PC] **Result:** `db-20260725-hubspend-r1` — MODEL SPEND moved to Hub (`#hub-spend-panel`); removed from Systems. Systems = power (honest N/A on Le Potato) + CPU/per-core + RAM/GPU/swap/load + storage + top procs; service DETAILS expand with about/restart/ports + local-ok vs edge-502 copy; pod-scheduler idle_ok when Hermes owns ticks. Parallelized `/api/systems` probes (was sequential hang → Loading…). Preserved Worker Goal/Step + work_pipeline. Potato deploy+restart :8790 200; smoke PASS (Hub spend 6 KPIs, Systems host KPIs, mobile Hub spend + Systems host). No commit.
- **2026-07-25T04:05Z** — [PC] **Result:** Re-merged Hub smoke asserts into `dashboard-ui-smoke.mjs` (Free HEAD ≥1 option + Worker Goal/Step collapse; kept Systems/spend). SCP → potato (hash `619abed9…`); node smoke **EXIT 0** Free head=3, Worker collapsed (6063 chars hidden). Race: runner's chromium install window lets siblings clobber file mid-run — prefer direct `node …/dashboard-ui-smoke.mjs`. No commit.
- **2026-07-25T04:01Z** — [PC] **Result:** Hub Free catalog restored on potato. On-disk was stale v1 (hy3+qwen only → CHAT_SUNSET → 0 free; Free UI survived only via inject). Deployed PC v3 `chat-catalog.json` (sha b3eb48e2495db5ac) free=[`poolside/laguna-xs-2.1:free`, `nvidia/nemotron-3-super-120b-a12b:free`, `nvidia/nemotron-3-ultra-550b-a55b:free`, `cohere/north-mini-code:free`, `inclusionai/ling-3.0-flash:free`] + paid DeepSeek/Hermes/GLM/Venice; scp'd `protected-runtime-paths.json` (chat-catalog now protected — was missing on potato). `sudo systemctl restart linuxbox-status`; `:8790` 200; GET `/api/model-budget` free_count=5 (catalog rows, not inject). Routing left free=[Laguna,Nemotron-super,Ling] — **did not** re-save kimi/qwen (sunset/probe-dead). `run-dashboard-visual-check.sh` **EXIT 0** Free head/fallback options=5. No commit.
- **2026-07-25T04:00Z** — [PC] **Result:** Owned Hub fixes verified live (`db-20260725-hubfix-r2`) via throwaway DOM probe (did NOT touch shared smoke.mjs — sibling owns it; spend stays off my scope, Hub per human). **Free HEAD** = [Ling, Nemotron-super, Laguna] (3), **Free FALLBACK** 3 options — server now injects free rows from routing/CHAT_FREE_LAST_RESORT when catalog has 0 free, so clobber can't blank them. **Worker Log** collapsed by default: Goal (`exit 124 · tableslop-progress.md`) + Step (live last tool line `┊ 💻 $ curl …:8765/health`); full 13.7KB log hidden, toggle → display:block. **502 permanence**: /proc process reads (no `ps` spawn) + SIGTERM clean shutdown — :8790 active 200. Sibling clobbered my smoke.mjs asserts; recommend re-merging Free-head + Worker-log asserts. No commit.
- **2026-07-25T04:00Z** — [PC] **Result:** Human **self-improvement / education force lane** wired. Spec `agents/SELF_IMPROVEMENT_TASK.md` + board `agents/self-improvement-progress.md` (6 open: si-math-a/b, si-speech-a, si-write-a, si-tech-a/b). Priority = CURRENT_TASK §9 / CLAUDE §10 — after ops+product+campaign, above IDLE. has-work marker last in THINK_MARKERS; think-tick free-first + inbox anti-spam (`si-*`, report-only if open). Deliver `reports/self-improvement/` and/or Hub Inbox. Distinct from AI-stack `SELF_IMPROVE_PROGRESS.md`. Deployed potato repo+`~/bin` (has-work/think-tick, CRLF stripped); has-work still WORK: open user-tasks (SI waits until quiet). No commit.
- **2026-07-25T03:59Z** — [PC] **Intent:** Hub Free catalog fix — potato `chat-catalog.json` re-clobbered to stale v1 (hy3+qwen → sunset-filtered to 0 free; Free UI only via server inject). SCP PC v3 catalog (Laguna/Nemotron/North/Ling free + paid) + `protected-runtime-paths.json` (add chat-catalog); restart linuxbox-status; verify `/api/model-budget` free rows + visual Free PASS. Will NOT re-add sunset kimi/qwen. No commit.
- **2026-07-25T03:57Z** — [PC] **Intent:** Design durable **human self-improvement / education force lane** (math, speech/writing, tech education). Files: `agents/SELF_IMPROVEMENT_TASK.md` + `agents/self-improvement-progress.md` (4–6 tick starters). Wire CURRENT_TASK / has-work / think picker **after ops+product+campaign, above IDLE**. Free-first; deliver via human-inbox (rich context, max 1/cycle) or `reports/self-improvement/`. Distinct from AI-stack `SELF_IMPROVE_PROGRESS.md` (S1–S3). Deploy markers to potato. No commit.
- **2026-07-25T03:58Z** — [PC] **Intent:** Dashboard UX — move MODEL SPEND/TOKEN USAGE from Systems → Hub; Systems = power+CPU/RAM/cores/storage/procs only; service cards expand with what/how blurbs + fix false local-ok→502; preserve Hub Worker Goal/Step; bump DASH_BUILD; deploy potato; smoke. Holder `hub-spend-systems-r1`. No commit.
- **2026-07-25T03:56Z** — [PC] **Result:** `db-20260725-hubfix-r2` — catalog re-clobbered to stale v1 mid-verify; re-deployed v3 + **server injects free rows from routing/CHAT_FREE_LAST_RESORT when catalog has 0 free** (so Free dropdowns survive clobber). Worker log Goal/Step collapse + /proc (no ps) 502 fix remain. Hard-start :8790 200; FREE=[Laguna,Nemotron×2,North,Ling]. Commit catalog+server+index still recommended.
- **2026-07-25T03:56Z** — [PC] **Result:** Visual parse inventory + live try. **No vision-LLM/OCR system** for Hub; canonical = Playwright C6 (`run-dashboard-ui-smoke.sh` / chat smoke). Added `run-dashboard-visual-check.sh` (Hub+Systems shots + failure greps). Live potato loopback: Hub **200**, no 502/load-failed; Systems Host **OK** (7 KPIs); **FAIL** Free HEAD/FALLBACK empty — catalog has 0 free-tier rows (routing still lists 3 free ids); Worker Log collapsed but 13k Hermes dump behind toggle. Report `reports/dashboard-visual-parse/LATEST.md` + shots `…/2026-07-25T03-54-56/`. No commit.
- **2026-07-25T03:52Z** — [PC] **Intent:** Inventory visual/UI parse (Playwright vs vision/OCR); try live potato Hub+Systems visual check; report under `reports/dashboard-visual-parse/`. No commit.
- **2026-07-25T03:53Z** — [PC] **Result:** Hub/Systems/Free fix `db-20260725-hubfix-r1`. **502 root:** systemd restart wedged on spawned `ps` children (final-sigterm timeout → :8790 down → tunnel HTTP 502 hub-wide). Fixed: top-process + process metrics via `/proc` (no `ps`), hermes D-state via `/proc/status`, SIGTERM clean shutdown. **Free dropdowns:** potato catalog was stale v1 (hy3+qwen only → sunset-filtered to 0 free); restored v3 free=[Laguna, Nemotron-super/ultra, North-mini, Ling] (not Kimi/Qwen — still CHAT_SUNSET / probe-dead). **Worker log UX:** Hub default = Goal (think-focus blurb/task) + Current step (last actionable think-live line); full log behind toggle. **Systems:** host CPU/cores/storage/procs + 8 services restored when API up. Catalog on protected-runtime-paths; **recommend commit** catalog+server+index to Linuxbox main. No commit.
- **2026-07-25T03:52Z** — [PC] **Result:** Durable `linuxbox-status` stop-fast — drop-in `scripts/linuxbox/linuxbox-status.service.d/stop-fast.conf` + `install-linuxbox-status-stop-fast.sh` (idempotent; daemon-reload only if needed; no restart). Applied on potato: DropInPaths=`…/stop-fast.conf`, KillMode=control-group, TimeoutStopUSec=10s, SendSIGKILL=yes; unit active, :8790 **200**. Also mirrored three lines into repo `linuxbox-status.service` template. PC+potato `hermesGatewayUp` uses `/proc/<pid>/status` (no `killSignal`/`ps` spawn). No commit.
- **2026-07-25T03:45Z** — [PC] **Result:** Hub 502 diagnose on potato. Loopback never JS-threw; origin was down during `linuxbox-status` stop hang (23:43:01–23:44:37 EDT): journal `State 'final-sigterm' timed out. Killing.` / `Failed with result 'timeout'` (stuck child `ps`), then SIGKILL restart churn `Failed with result 'signal'`, auto-restart ActiveEnter 23:44:37. No server.js syntax fix needed. Post-check: `/` `/api/agent` `/api/systems` `/api/machines` `/api/model-budget` all **200**. No commit.
- **2026-07-25T03:40Z** — [PC] **Intent:** Diagnose+fix Hub Chat Models empty FREE HEAD/FALLBACK dropdowns (PAID MINOR still DeepSeek). Check potato `/api/model-budget` catalog+routing vs on-disk `chat-catalog.json` + DASH_BUILD vs repo; restore free options; protect catalog from git-bundle revert; verify Playwright. No commit.
- **2026-07-25T03:35Z** — [PC] **Result:** abhinavall cleanup naming durable — `abhinavall_check.sh` now calls hyphen `abhinavall-cleanup.sh` (repo renamed; install source path updated). SCP	o potato `~/.hermes/scripts/` + `~/agent-dump/scripts/website/`; check EXIT 0 with and without underscore symlink. No commit.
- **2026-07-25T03:32Z** — [PC] **Intent:** Make abhinavall cleanup naming durable — check calls hyphen `abhinavall-cleanup.sh` to match install	o ~/.hermes/scripts/; rename repo source; SCP + verify EXIT 0. No commit.
- **2026-07-25T03:22Z** — [PC] **Result:** Seeded tableslop board ts-e–g (region cast chips, sheet markdown, portrait create/handoff); synced `agents/tableslop-progress.md` to potato.
- **2026-07-25T03:21Z** — [PC] **Result:** Stream B — cast on tableslop. Read-through `GET /api/characters` + `/api/characters/image` on `:8765` (same `characters-registry.json` + disk portraits). HUD **Cast** / `#cast`. Dashboard Chars thin link “Open on map →” (`db-20260725-chars-cast-r1`). Union-merge PC∪potato under lock `chars-tableslop-stream-b` → registry **v29** (added alisa/celine/sofia/harper-sister/jinpei); lock **released**. Verify: loopback health cast:true; public `https://map.tableslop.org/#cast` count=15 with_image=12; Celine PNG 200. Leftover: create/merge/upload/Discord-resolve stay on :8790; region↔character_ids pins not wired; discord-export enrich not on tableslop. No commit.
- **2026-07-25T03:37Z** — [PC] **Result:** Stream A dashboard shipped (`db-20260725-spend-systems-r1`, server+index scp'd potato, `linuxbox-status` restarted, `:8790` 200). (1) **Spend tracker** on Systems page (`#sys-spend-panel`, `renderSpend()`) — REAL data from `/api/agent`: policy (free-first), ops $ **target** ($7/day, labeled policy cap not measured), UTC usage day, **free vs paid call attempts** + free-first utilization bar, per-model table (calls/OK/fail/429+cap/last). Added `rate_limit` to server `chatModelUsageSummary`. Honest note: counts are call attempts (`chat-model-usage.json`), NOT billed tokens — OpenRouter/ZenMux per-call $ isn't exposed. Today shows 0 calls (think free-only/paused, chat idle). (2) **Moved CPU/hardware off Hub**: removed `#hub-resources` CPU/RAM/GPU bars + resources modal + per-core, killed the 1Hz `/api/host-resources` poll (`shouldPollHubResources`→false), trimmed Swap+Disk-free dup KPIs (kept USB+API keys). Full host view (per-core CPU, storage, top procs) stays Systems-only. (3) **Mobile** CSS for spend panel; extended `dashboard-ui-smoke.mjs` (+systems tab, spend-panel, hub-bars-removed, systems-mobile). Smoke EXIT=0: `spend panel present (6 KPIs)`, `systems-mobile no overflow`, `spend panel fits viewport`, `no console errors`. Also fixed a **pre-existing** unrelated console throw (`setupReportKeyboardNav` undefined on Reports tab) with a forward-compatible `typeof` guard. **RACE WARN:** a concurrent repo sync reverted my scp'd smoke `.mjs` mid-verify (re-deployed); index/server are scp-only overlays (uncommitted per "no commit") — a `git reset --hard` bundle apply could revert them. Recommend committing to Linuxbox `main` to make durable. No commit.
- **2026-07-25T03:15Z** — [PC] **Intent:** Stream A — dashboard. (1) Add a **model spend / token-usage** tracker to the **Systems** page from REAL data already in `/api/agent` (`chat_model_usage` attempts/ok/fail/429/daily-cap + `model_budget` free-first policy + ops USD *target*) — no invented dollars/tokens. (2) Move **CPU/hardware** off **Hub** to Systems-only: remove Hub `#hub-resources` CPU/RAM/GPU bars + resources modal, trim Swap/Disk-free dup KPIs, stop the 1Hz hub-resources poll (Systems host panel already has per-core CPU + storage + top procs). (3) Mobile CSS to match. Bump `DASH_BUILD` server+index; deploy potato; restart `linuxbox-status`; verify `:8790` 200 + mobile smoke. Free models only for any agent LLM; code not paid loops. No commit.
- **2026-07-25T03:16Z** — [PC] **Intent:** Stream B — combine characters onto tableslop (`map.tableslop.org` / `:8765`). LOCK claim: chars-registry:tropic-gooner (read-through first; acquire disk lock before any write). Serve same `campaigns/tropic-gooner/characters-registry.json` + portraits from tableslop Cast panel; dashboard Chars stays data/admin (thin link, no wipe). Holder `chars-tableslop-stream-b`.
- **2026-07-25T03:18Z** — [PC] **Result:** Continuous tableslop/map lane wired. Gap: manifest JSON + mega user-tasks never fed has-work checkboxes; picker ignored USER_TASKS product preference (any open task beat map). Added `agents/tableslop-progress.md` (ts-a..d), free-first in `TABLESLOP_PROJECT_TASK.md`, THINK_MARKERS + CURRENT_TASK 5a, think picker: `[ops]` → product boards → product tasks → else. Deployed `~/bin` + repo on potato; has-work WORK; picker→ts-a when no ops. Fixed trailing comma in `user-tasks.json`. No commit.
- **2026-07-25T03:16Z** — [PC] **Intent:** Stream C — continuous project/map lane. Inventory lane pickers; add durable tableslop tick board (`agents/tableslop-progress.md` + wire has-work/think picker after `[ops]`, before idle); free-first in task prompt; seed 2–4 map slices; deploy tick scripts to potato `~/bin`; verify has-work. No commit.
- **2026-07-24T22:52Z** — [PC] **Result:** Pixi RP OpenRouter key split done on potato. User had pasted new RP key on line 21 but lines 49–50 still overrode with old shared ops key. Promoted RP key (`fp 1b6be9121ccd`) to both `OPENROUTER_API_KEY` + `WRITER_BOT_…`, dropped duplicates; Hermes ops stays `fp 3f76e2d1290a`. Backup `~/.linuxbox-pixi/deckard-local.env.bak.pre-rp-split.*`. Restarted `linuxbox-pixi-rp` active. Secrets not logged.
- **2026-07-24T22:50Z** — [PC] **Intent:** User added Pixi RP OpenRouter key to potato `~/.linuxbox-pixi/deckard-local.env`. Finish: sync WRITER_BOT key if needed, chmod 600, restart `linuxbox-pixi-rp`, verify :8767 + key split vs Hermes ops (hashes only). No commit.
- **2026-07-24T22:20Z** — [PC] **Result:** Linuxbox Step burn stopped. Chart Jul 23–24 (~$12–16/day Step) was the old think primary thrashing every minute — **live profiles already free-first, zero Step in `~/.hermes/profiles/*/config.yaml`**. Hardened further: (1) `stepfun/step-3.7-flash` added to dashboard `CHAT_SUNSET_MODELS` (same filter as hy3 — cannot be selected even if someone puts it back in config); (2) removed from `paid_models_ops`; (3) sunset entry in model-budget; (4) **think crontab PAUSED** (fast left running) — marker `agents/state/think-paused.json`; resume with `bash ~/bin/resume-agent-ticks-adaptive.sh` (think is now free-only + 8m throttle). Dashboard restart 200; `/api/model-budget` paid_ops has no Step. Today's chart bar is mostly pre-fix spend — new spend should be ≈$0 on the always-on lane.
- **2026-07-24T22:16Z** — [PC] **Intent:** Chart shows Step ~$12–16/day Jul 23–24 on linuxbox. Verify live profiles already free-first; purge Step from remaining selectable paths (`paid_models_ops`, dashboard fallbacks); confirm no live process still calling stepfun; harden so Step cannot be re-selected. No commit.
- **2026-07-24T22:10Z** — [PC] **Result:** Model chains rebuilt **free-first** (user: free models; GLM 5.2 only as DeepSeek backup). **Two dead ids found by live probe:** `zenmux:moonshotai/kimi-k3-free` = **404 invalid_model, never existed** (real `moonshotai/kimi-k3` is PAID $3/$15 per M — NOT a free option); `qwen/qwen3-next-80b-a3b-instruct:free` = **delisted** from OpenRouter. Both were in dashboard Chat `free_models`, so **every Chat turn fell through to paid** — second cost leak beside Step. New chains (probe-verified, 6/8 free ids live): think **FREE-ONLY** Laguna→Nemotron-super→Ling; fast Laguna→Ling→DeepSeek; meta/code `cohere/north-mini-code:free`→Nemotron-ultra→DeepSeek→GLM 5.2; chat Laguna→Nemotron-super→DeepSeek→GLM 5.2. **ZenMux cannot be a profile primary** (Hermes resolves it as provider `custom`, drops key → 403) — registered for manual `zenmux:<slug>` only. Updated live profiles + installer (variadic fallbacks) + registry (`dead_models`) + model-budget config + chat-catalog v3 + dashboard JS (Step removed from paid chain, `CHAT_PAID_MID`=GLM 5.2) + AGENTS.md + CLAUDE.md. New probe tool `.staging/model-probe/probe_free_models.py`. Verified: think/meta smokes OK, dashboard restart 200, `/api/model-budget` now serves live free ids. Profile backups `/tmp/hermes-profile-bak/`.
- **2026-07-24T21:57Z** — [PC] **Result:** Think lane hardened vs cost drain (user: Step burned ~$14). (1) **Cadence:** `THINK_INTERVAL_SEC` default 10s→**480s** in `agent-cycle-think-tick.sh` (active `~/bin` + repo) — cron still fires 1/min but the LLM only runs every ~8m; cheap has-work poll still short-circuits when idle. (2) **Free-only think:** live `~/.hermes/profiles/think/config.yaml` primary `poolside/laguna-xs-2.1:free` → Qwen free, **no paid model** (fails/IDLEs rather than spend). DeepSeek removed from think chain everywhere; chat keeps DeepSeek last resort. Synced installer / `pc-bonsai-routing.json` / `hermes-model-registry.json` / failover floor / CLAUDE.md. Verified: INTERVAL_SEC=480 both copies, bash -n OK, live chain free-only, `think-free-ok` smoke on Laguna.
- **2026-07-24T21:49Z** — [PC] **Result:** Think lane → **FREE-FIRST** (was bleeding ~$14/day: Step paid primary on an every-minute `* * * * *` tick, thrashing dashboard-UI backlog 3×/item). Live `~/.hermes/profiles/think/config.yaml` + chat: primary `poolside/laguna-xs-2.1:free` → Qwen free → DeepSeek (paid last resort only). Dropped `zenmux:moonshotai/kimi-k3-free` (403 no-access = dead billed hop). Synced installer / `pc-bonsai-routing.json` / `hermes-model-registry.json` / failover floor / CLAUDE.md. Smoke: `think-free-ok` on Laguna free. NOTE: root drain is cadence×paid; cadence still 1/min — offered to widen.
- **2026-07-24T21:45Z** — [PC] **Result:** Think primary → `deepseek/deepseek-v4-flash` (Step demoted). Live `~/.hermes/profiles/think/config.yaml` patched; fallbacks GLM + ZenMux Kimi. Synced installer / `pc-bonsai-routing.json` / `hermes-model-registry.json` / failover floor. CLAUDE.md table updated. Next think tick uses DeepSeek.
- **2026-07-24T21:42Z** — [PC] **Intent:** Demote Step from Hermes **think** primary → `deepseek/deepseek-v4-flash` (Step was wrecking think ticks). Update installer, `pc-bonsai-routing.json`, `hermes-model-registry.json`, live potato think config. No full `install-hermes-profiles.sh` re-run. No commit.
- **2026-07-24T21:55Z** — [PC] **Result:** Hub Chat Models — Kimi K3 now **Free-only**. Two root causes: (1) server paid-failover/lane/pinned/timeout checks used `includes(":free")` only → ZenMux `-free` misclassified as paid (UI `isHubFreeModelRow` + save already fixed in r1); unified all on shared `isChatFreeModelId`/`isHubFreeModelRow` (free dropdown, paid exclusion, failover chain, save validation). (2) **potato on-disk `chat-catalog.json` was STALE** (6 models, still had sunset `tencent/hy3:free`, **no Kimi/Laguna/Step**, mtime 16:56 — reverted by an earlier deploy/agent race); r1 only served 8 models from memory cache, so my restart exposed the stale file and Kimi vanished. Backed up stale → deployed PC 8-model catalog (Kimi `tier:free`, no hy3). Live verify `:8790` GET: 8 models, ALL_TIERS_CONSISTENT; free dropdown = [laguna, qwen, kimi], paid = [deepseek, step, hermes, glm, venice]; `kimi in FREE:true / PAID:false`. **free_models** = `["zenmux:moonshotai/kimi-k3-free","qwen/qwen3-next-80b-a3b-instruct:free"]`; **paid_minor** deepseek-v4-flash, **paid_mid** glm-5.2. Dash **db-20260724-kimifree-r2** (server+index+catalog scp'd, `linuxbox-status` restarted, node --check OK). No commit. Hard-refresh Hub. NOTE: stale-catalog revert is a recurring deploy race — chat-catalog.json is untracked on PC; needs a durable source.
- **2026-07-24T21:40Z** — [PC] **Intent:** Fix Hub Chat Models — Kimi K3 free (ZenMux `-free`) shows under Paid fallback, not Free. Root: UI dropdown + paid failover chain still used `includes(":free")` only (missed ZenMux `-free`). Unify on shared free classifier (`isHubFreeModelRow` UI / `isChatFreeModelId` server) for free dropdown, paid exclusion, failover chain, save validation; ensure free=[kimi,qwen], paid=real paid (DeepSeek/GLM); bump DASH_BUILD; deploy potato; verify GET `/api/model-budget`. No commit.
- **2026-07-24T21:25Z** — [PC] **Result:** Pixi degenerate spiral forensics + gate. Session c5409afe msg80 = DeepSeek glued/no-space prose; chain primary→continuation→replay→footer-repair, finish_reason=stop, anti-loop missed. Archives /mnt/archive/logs/pixi-degenerate/ (+ PC reports/pixi-degenerate/). Fix rev 20260724-degenerate-gate-v1: detect glue/panic loops; post-hop rewrite-or-refuse; persist finish_reason on assistant msgs. Hard-refresh Pixi.


- **2026-07-24T21:07Z** — [PC] **Result:** Split oversized think-lane backlog items + synced potato. `LINUXBOX_DASHBOARD_BACKLOG.md`: Hub mega-bullet → Hub-a/b/c/d (+ Smoke); synced 9 stale potato Open items that were already Done on PC into Done. `system-integrity-progress.md`: 1.1 → 1.1a/b/c. SCP both files; sha256 match PC↔potato. Reset `agents/state/think-lane-attempts.json` to `{}`. Next think first box = Hub-a. No commit.

- **2026-07-24T21:06Z** — [PC] **Intent:** Split oversized open backlog bullets (esp. Hub lane activity chips on potato `LINUXBOX_DASHBOARD_BACKLOG.md`) into tick-sized `[ ]` sub-items so think can finish one per cycle; sync potato↔PC; optional reset `think-lane-attempts.json`. No wipe, no commit.

- **2026-07-24T21:00Z** — [PC] **Result:** Pixi prior-beat anti-echo on potato `:8767` rev `20260724-prior-beat-v1`. DeepSeek profile `prior_beat_similarity_max=0.58` / word_run=10 (was global Send 0.86 — near-copies slipped). Advance-only inject; skip provisional in prior compare; stronger replay rewrite. Hard-refresh Pixi.

- **2026-07-24T21:03Z** — [PC] **Result:** Think-tick fix deployed to potato `~/bin/agent-cycle-think-tick.sh` + repo. **Root cause:** picker read only `user-tasks.json`; all 7 tasks `blocked` while `LINUXBOX_DASHBOARD_BACKLOG.md`/`system-integrity-progress.md` had open `[ ]` → has-work said WORK, picker returned empty task → vague `"ONE step for task : think lane"` → agent grep/read-wandered to 18/18, empty task_id made enforce a no-op, re-fired every ~60-90s forever (0 completions). **Fix:** (1) thread has-work marker into picker → concrete `Lane <file>: <item>` prompt + completion contract (flip `[ ]`→`[x]` + dated Done line); (2) `enforce-lane` safety net flips the exact handed item if log ends `DONE:` (tested: flips only matching box, safe no-op otherwise); (3) anti-thrash attempt counter (`agents/state/think-lane-attempts.json`, `THINK_MAX_LANE_ATTEMPTS=3`) skips a too-big box after 3 tries so it can't jam the lane (tested: 3× then rotate); (4) ceilings 18→20 / 24→28, timeouts 210→240 / 270→300; ops regex now matches lane/integrity/Meta; (5) tighter prompt (implement>diagnose, "don't re-read"). **Verified live:** post-deploy tick shows concrete lane blurb + ops budget 28/300; a run hit 300s timeout and the trap recovered focus→`failed` (hang guards intact); `:8790`=200. **Residual:** first backlog item is a large multi-step feature that times out per tick — anti-thrash rotates off it after 3 tries, but oversized backlog bullets should be split into tick-sized sub-items; cross-*file* lane rotation not added (within-file only). No commit (not requested).

- **2026-07-24T20:52Z** — [PC] **Intent:** Fix think-tick 18/18 thrash + no-completion. Root cause: picker reads only `user-tasks.json`; all tasks blocked but `system-integrity-progress.md` open → has-work says WORK, picker returns empty task → vague "think lane" prompt → wander to ceiling, no task_id so enforce no-op, re-fires forever. Fix: thread has-work marker into a concrete lane-work prompt + `[ ]`→`[x]` completion + `enforce-lane` safety net; bump ceilings (18→20 / 24→28); tighten prompt. Deploy `~/bin/agent-cycle-think-tick.sh`.

- **2026-07-24T20:47Z** — [PC] **Intent:** Hub Chat Models — set free_models main=Kimi K3 (`zenmux:moonshotai/kimi-k3-free`), backup=Qwen; keep prefer_free; fix allowlist if Save drops ZenMux -free.
- **2026-07-24T20:52Z** — [PC] **Result:** Hub Chat free routing → `["zenmux:moonshotai/kimi-k3-free", "qwen/qwen3-next-80b-a3b-instruct:free"]` (prefer_free true; paid DeepSeek / GLM kept). Fixed ZenMux `-free` allowlist (Save + Hub dropdown + failover chain ignored Kimi before). Catalog synced; dash `db-20260724-kimifree-r1`; GET+POST :8790 verified.


- **2026-07-24T01:35Z** — [PC] **Result:** Pixi speech-vs-narration contract on potato `:8767` rev `20260724-speech-narration-v1`. New inject `[Player input — speech vs narration]`: quoted = PC speech; unquoted questions = authorial description/direction (not NPC Q&A). Direction contract + NSFW minimal system aligned; deep-description detector covers `what does she look like?`-style asks. Hard-refresh Pixi.

- **2026-07-24T01:30Z** — [PC] **Intent:** Pixi — teach model to differentiate **quoted dialogue** vs **unquoted narration/description asks** (questions in player text are authorial direction, not diegetic questions to NPCs). Patch potato `:8767` inject.

- **2026-07-24T01:23Z** — [PC] **Result:** Wired **research-bookmarks** source SoT `agents/research-bookmarks-source.json` → ingest `reports/research/bookmarks-inbox.json` (example + `scripts/pc/write-bookmarks-inbox.py`; no X API). Taught CURRENT_TASK 7b / USER_TASKS / RESEARCH_BOOKMARKS_TASK. Potato cron live; digest path verified with fixture; no live login — Inbox `research-bookmarks-weekly` open for PC login/export. Not News/Intel RSS.
- **2026-07-24T01:19Z** — [PC] **Intent:** Wire X bookmarks as source for research-bookmarks lane (no X API). Inventory → config+PC ingest path → teach agent → verify; deploy potato only if needed.
- **2026-07-24T00:46Z** — [PC] **Result:** Guardrails/smoke verify — `agents/THINK_SECURITY_CHECKS.md` on PC+potato; think tick soft~12 / hard 18|24; Chat UI smoke **PASS** (`reports/chat-ui-smoke/LATEST.md` on potato); `:8790` 200. Closed user-task `eb46c98d` `[ops] Chat not running` blocked→done (smoke evidence).
- **2026-07-24T00:30Z** — [PC] **Intent:** Soften think/worker guardrails into staged security checks; teach agent pass-path; verify smooth run.
- **2026-07-24T00:45Z** — [PC] **Result:** Pixi permanence fix on potato `:8767` rev `20260724-permanence-v1`. Root causes: (1) inventory list merge via `_shallowMergeObjects` wiped arrays to `{}`; (2) package seed only role/age — now seeds Appearance/Demeanor/Kit → appearance_notes/personality/inventory; (3) present-cast inject skipped body/kit — now injects appearance + inventory + held; (4) salvage inventing military NPCs / empty objects — refresh-only + prose object/outfit salvage + inventory→objects mirror. Selfcheck OK; active session Vivian seeded; hard-refresh Pixi.

- **2026-07-24T00:35Z** — [PC] **Intent:** Pixi permanence gap — physical descriptions, inventory, objects, personality/persona not preserved. Diagnose merge/sheet/inject path on potato; fix so established facts stick (no hardcodes).

- **2026-07-23T04:15Z** — [PC] **Result:** Session close — Pixi gobstopper design locked (`docs/pixi/ENGINE-GOBSTOPPER.md` + `.mem-constant/pixi-engine.md`). **Hard regen** shipped on potato (edit last user + drop assistant + fresh Send; paren OOC). PC shutdown scheduled `shutdown -s -t 1200`.

- **2026-07-23T04:02Z** — [PC] **Result:** Pixi **engine gobstopper** design SoT at `docs/pixi/ENGINE-GOBSTOPPER.md` (UI+engine, peelable layers, delaying gratification north star, anti-hardcode, package plug-in). mem pointer `.mem-constant/pixi-engine.md`; linked from `pixi-rp-context-agency-plan.md` + `docs/pixi/README.md` + AGENTS Pixi pref. No runtime code this pass.
- **2026-07-22T02:25Z** — [PC] **Result:** Pixi list-format examples + merge hygiene on potato. Inject `list_format` / list `direction_contract` few-shot table (1|Name|Age|Notes); continuation = next numbered rows only + N+1 nudge; merge strips duplicate `[Date…]`, mid-list closers, `5-10:` groups. Tests 37/37 + node 3/3; rev `20260722-list-format-examples-v1`; :8767 **200**. Hard-refresh Pixi; re-ask full dossier.
- **2026-07-22T02:15Z** — [PC] **Intent:** Pixi list-continue format examples + merge hygiene. Inject few-shot table format (one row/person); continuation = next numbered rows only (no re-header / Candy closer / group summaries); strip duplicate `[Date…]` headers + repeated closers on hop merge. Potato chat-ui; tests; deploy; cache rev.
- **2026-07-22T00:50Z** — [PC] **Result:** Pixi list-continue forensic v2 on potato. Pipe-cell row count (not only `|`-leading); skip quality ≤140/>180 on list/dossier; keep continuing when rows ≪ claimed/asked (stop≠complete); provisional persist won’t clobber longer final + preserve overwrites content. Tests 25/25 + node 2/2; revision `20260722-list-continue-v2`; :8767 **200**. Hard-refresh Pixi; re-ask full dossier.
- **2026-07-22T00:37Z** — [PC] **Intent:** Pixi list-continue forensic (session 14af5412) — v1 live but failed: inline pipe-row count=0 bail, quality ≤140 crush on dossiers, stop+WORLD_DELTA with ≪rows, provisional primary stuck after final Save. Fix detectors/quality skip/list completeness/provisional overwrite on potato chat-ui; tests; deploy.
- **2026-07-24T00:25Z** — [PC] **Result:** Worker thrash = think looping open `[ops] Chat not running` after API already proven OK (said BLOCKED in prose, left `status=open`). Killed spin; set task **blocked**; closed probe task. Fixed real bug: POST `/api/model-budget` used undeclared `body` → moved into `readBody` block; dash `db-20260724-modelbudget-r1`; POST verify 200. Think prompt now requires `status=blocked` not leave-open. Next open ops: Timeout times lane / smoke-test complaint.
- **2026-07-24T00:25Z** — [PC] **Intent:** Check Hub worker log / think-last; diagnose and fix whatever is failing.
- **2026-07-24T00:20Z** — [PC] **Result:** SpaceQuest archived to USB PERSONAL `exports/spacequest-archive-20260724T0005Z/spacequest-20260724T0005Z.zip` (sha256 `d90178e1…`, verified). Potato live `campaigns/spacequest` removed; stub `campaigns/SPACE QUEST-ARCHIVED.md`; lane skipped in CURRENT_TASK; pod stays paused. **PC tree:** still present until you confirm delete.
- **2026-07-24T00:15Z** — [PC] **Intent:** Archive SpaceQuest (campaign done) → zip onto linuxbox USB PERSONAL; then disable pod / ask before deleting live tree.
- **2026-07-24T00:12Z** — [PC] **Result:** Hang loop was browser_navigate→60s + outer timeout 124/SIGINT, not approvals. Think tick now `--max-turns 12`, timeout 180s, toolsets without browser; EXIT trap clears stuck Hub focus; Chat task body corrected (`/api/chat/status` ≠ health). Hard-refresh Hub.
- **2026-07-24T00:10Z** — [PC] **Intent:** Think still hangs (~4m) then Interrupt — browser_navigate :8790 60s + SIGINT. Ban browser on ops ticks; trap cleanup for focus; clarify Chat task if API already up.
- **2026-07-24T00:05Z** — [PC] **Result:** Root cause: `hermes chat` sets `HERMES_INTERACTIVE=1` → defeats `cron_mode=approve` (60s Timeout-deny). Think tick now `chat --yolo` + logged form (`agents/state/shell-access-forms/`) + archive MD (`/mnt/archive/logs/think-reports/`, `reports/think-ticks/LATEST.md`). Doc: `docs/ops-think-shell-access.md`. Hardline still protects wipe/shutdown.
- **2026-07-24T00:00Z** — [PC] **Intent:** Hermes still Timeout-denying systemctl after cron-session fix. Investigate approvals path; add logged shell-access form + archive markdown reports for self-work (safe, reversible).
- **2026-07-23T23:56Z** — [PC] **Result:** Hermes “Timeout — denying command” = `approvals.mode=manual` 60s with no human /approve (think `chat -q` was not a cron session). Fixed: think tick exports `HERMES_CRON_SESSION=1` → `cron_mode=approve`. Prompt prefers curl :8790 over systemctl.
- **2026-07-23T23:55Z** — [PC] **Intent:** Explain Hermes “Timeout — denying command” on systemctl; check approvals / think prompt so ticks don’t hang 60s.
- **2026-07-23T23:32Z** — [PC] **Result:** Hub observability expanded `db-20260723-obsexpand-r1`: tall Worker log pane always on Hub; **Last think** (full blurb) above log; Fast ticks demoted to **Heartbeats**. Poll think-live on Hub idle too. Hard-refresh.
- **2026-07-23T23:30Z** — [PC] **Intent:** Expand Hub Running now / Last run observability (screenshot) — show think work + richer live pane, not only Fast heartbeats.
- **2026-07-23T23:26Z** — [PC] **Result:** Think cooldown **10s** after job end (was 300s from job start). Self-requeue if more open work. Cleared `think-llm.last`. Hub should show wait Ns/10s max.
- **2026-07-23T23:25Z** — [PC] **Intent:** Think throttle too long (wait 120s/300s). Cap post-job wait at ~10s.
- **2026-07-23T23:20Z** — [PC] **Result:** Hub live observability `db-20260723-obslive-r1`: Running now shows think-last.log window (poll `/api/think-live` ~2.5s); kpi Gateway/Inbox/Fast/Think strip compacted; think tick line-buffers via tee; killed hung think (denied sudo). Hard-refresh Hub.
- **2026-07-23T23:15Z** — [PC] **Intent:** Hub observability — think tick looks stuck; expand Running now with live activity window; shrink gateway/error/fast/think strip. Diagnose potato think first.
- **2026-07-23T23:10Z** — [PC] **Result:** Locked direction — multi-subagents free×2–3 + paid×1; Lineage = Termux free worker (B). Plan: `docs/plans/parallel-free-workers-lineage-2026-07-23.md`. News still 18 RSS; awaiting human buckets before adding feeds.
- **2026-07-23T23:05Z** — [PC] **Intent:** Human offers LineageOS Android as extra compute (Tailscale-connectable). Clarify role vs Pixel-3a reader + potato before any phone agent install.
- **2026-07-23T23:00Z** — [PC] **Result:** Reframed `3ba53e19…` as normal ops investigate/fix (not self-heal drill). Human asks: can we parallelize 2–3 agents (1 paid + free until limit)? Survey swarm `max_concurrent` + model-budget before proposing.
- **2026-07-23T22:48Z** — [PC] **Result:** Ops task `3ba53e19-1226-4960-8176-f4838697edf4` queued for Chat Models Save routing error; `think-focus` set. Likely root: POST `/api/model-budget` undeclared `body` in server.js ~6429.
- **2026-07-23T22:55Z** — [PC] **Intent:** File Hub Chat Models “body is not defined” as soon `[ops]` user-task for investigate/fix.
- **2026-07-23T22:50Z** — [PC] **Result:** Simplified ticks **work**: crontab → durable `~/bin/agent-cycle-{fast,think}-tick.sh` (survives git reset that kept reverting repo scripts). Fast: no Hermes when inbox idle. Think: 5m throttle + `think-focus.json`. Pod-scheduler stays off. Hard-refresh Hub.
- **2026-07-23T22:45Z** — [PC] **Intent:** Simplify think/fast ticks; verify they actually work (no dual-fire, focus visible, throttle holds).
- **2026-07-23T22:40Z** — [PC] **Result:** Thrash root causes: (1) potato still had **old fast-tick** (always Hermes LLM); (2) **system** `agent-pod-scheduler.timer` dual-fired Think pod beside crontab; (3) think 1m with 22 open tasks + no focus log. Fixed: deploy gated fast/think (5–10m throttle), `think-focus.json` → Hub Running now, stop/disable system pod-scheduler, mobile Page select + Meta, dash `db-20260723-thinkfocus-r1`. Hard-refresh `/Linuxbox/`.
- **2026-07-23T22:30Z** — [PC] **Intent:** Diagnose pods stuck “continuously thinking,” no visibility into work, tasks piling up, mobile view not updated. Evidence from potato before changing anything.
- **2026-07-23T04:35Z** — [PC] **Intent/Result:** Human: shut down PC when done. Pixi lane wrap complete; initiating Windows shutdown (~90s — cancel: `shutdown /a`).
- **2026-07-23T04:30Z** — [PC] **Result:** Pixi linuxbox lane live. Shipped `agency_delay` (**mystery≠confusion**) rev `20260723-agency-mystery-v1` on `:8767`; free-first Laguna + paid fallback off (env bak). Project `pixi-rp` + tasks; next **pixi-03 turn-job**. Hard-refresh UI.
- **2026-07-23T04:20Z** — [PC] **Intent:** Continue Pixi RP development (linuxbox lane like tableslop): register project/tasks; encode **“mystery exciting / confusion frustrating”** into Gobstopper; one concrete engine/UI slice on potato `:8767` (no secret dumps).
- **2026-07-23T04:15Z** — [PC] **Intent:** Human: **also develop the Pixi system** (alongside tableslop/portfolio/self-improve). Survey potato `:8767` + open gaps; propose one next slice before large edits.
- **2026-07-23T04:05Z** — [PC] **Result:** Revert path: `docs/ops-revert-guide.md` + `pause-agent-ticks.sh` / `resume-agent-ticks-adaptive.sh`; S1 guard no longer pkills chats; git tag `restore-2026-07-23-adaptive-ticks` on Linuxbox remote after commit.
- **2026-07-23T04:00Z** — [PC] **Intent:** Human: don't randomly break things; keep **git/gitea revert** option. Snapshot commit + confirm rollback path for tick/S1/bookmarks/tableslop changes.
- **2026-07-23T03:56Z** — [PC] **Result:** Potato ticks **re-enabled** (fast 1m + think adaptive; pod-scheduler stays off). S1 DB guard live. Bookmarks cron **/3d + Sun**; inbox `research-bookmarks-weekly` answered (no API). tableslop v1 + portfolio P1–P4 options doc. **Next:** S2 coding+smoke; human pick portfolio present mode; tableslop `ts-game-02`.
- **2026-07-23T03:55Z** — [PC] **Result:** Adaptive think tick + S1 `hermes-profile-db-guard.sh`; bookmarks ~3d/no API; tableslop v1 locked (map+cast+session/Discord); portfolio presentation options at `docs/plans/portfolio-presentation-options.md` (pick P1–P4). Deploying ticks to potato next.
- **2026-07-23T03:47Z** — [PC] **Intent:** Execute human answers: re-enable **adaptive** ticks (5–10m think, 1m if backlog); **S4** self-improve order (S1 DB guard → S2 coding verify → S3 meta-harness); tableslop v1 = map+regions+cast+**session/Discord**; portfolio options + **presentation/review** surface; bookmarks **no X API**, ~**3–4 days** / ask login; paid OK for plan/complex. Limit idle thrash.
- **2026-07-23T03:41Z** — [PC] **Intent:** Goal align — human: ask follow-ups/guidance freely; **self-improvement of the AI system is tantamount** (above idle product thrash). Capture preference; propose self-improve lane; await answers on re-enable + bookmarks + what “self-improve” means this week.
- **2026-07-23T03:42Z** — [PC] **Result:** Idle ≠ empty work. **Root:** Hub thrash fix left `agent-cycle-*` crontab **paused** + `agent-pod-scheduler.timer` **disabled**. Added projects/tasks: tableslop **full game**, **abhinavall-portfolio**, **research-bookmarks**; Sat cron `research-bookmarks-weekly.sh` + Inbox ask `research-bookmarks-weekly`. CURRENT_TASK portfolio not USB-only. **Await human:** re-enable think/fast? bookmarks ingest A/B/C?
- **2026-07-23T03:37Z** — [PC] **Intent:** Diagnose agent idle/waste — was work paused (Hub fix) or truly empty? Ensure lanes cover (1) tableslop→full game project, (2) portfolio/blog depth, (3) weekly Sat X bookmarks research (@Wholesomeboi). No secrets; plan before re-enable storms.
- **2026-07-22T03:15Z** — [PC] **Result:** Mazda3 Chat→build memory. `parts.json` now has `vehicle.chassis=mazda3` + CP23 wheels **ordered**; prompt injects **[Confirmed build memory]**; user lines auto-patch via `extractMazdaFactsFromUserText`; bot can `PROJECT_WRITE` fences. Deployed server+parts to potato; :8790 up. Send again in Mazda3 thread — should stop re-asking chassis.
- **2026-07-22T03:10Z** — [PC] **Intent:** Mazda3 Chat → durable build memory. Chassis/wheels/etc confirmed in Chat must write to project tracker (parts.json / SoT) and re-inject next turn so model stops re-asking. Slice A only.
- **2026-07-22T00:18Z** — [PC] **Intent:** Dashboard Chat gaps — (1) talk-to-builds/etc doesn’t save context; (2) agent doesn’t self-modify code + test on website. Diagnose chat-threads persist + coding mode; clarify scope before large change.
- **2026-07-22T00:08Z** — [PC] **Result:** Hub stuck ≠ potato API (loopback/8780+Basic **200 ~10–20ms**; public URL **403** without CF Access JWT). Root likely browser hang on `/api/session` (no timeout) or Access/Basic. Patched Hub: Booting→session 10s abort→agent 20s abort + clear 401/403 text; dash-build `db-20260722-hubload-r1`; restarted :8790. **Hard-refresh** — should leave Loading within 20s with Hub or error string.
- **2026-07-22T00:05Z** — [PC] **Intent:** Hub STILL Loading after timer pause. Trace browser path (base URL, /api/agent via :8780+CF, auth, JS). Fix stuck Loading if code bug; evidence before claim.
- **2026-07-21T23:09Z** — [PC] **Result:** Hub still Loading because potato CPU storm kept returning. Root2: **system** `agent-pod-scheduler.timer` (every 30s) + crontab agent-cycle lines + bloated DBs. Actions: archived DBs earlier; **paused crontab agent-cycle-***; **sudo disable/stop agent-pod-scheduler.timer**; killed verify/hermes chat spam; restarted :8790. `/api/agent` ~10ms. Hard-refresh Hub. Re-enable timer/cron only after dedupe + DB size guard.
- **2026-07-21T22:50Z** — [PC] **Result:** Hub hang = potato thrash. Root: Hermes profile DBs **fast 2.3G** + **think 811M** + stuck think tick ~21h + overlapping lane chats. Killed stuck ticks; archived DBs → `/mnt/archive/logs/hermes-state-db-20260721T2247Z/`; fresh fast `state.db` 4K. `/api/agent` **200** in **16ms**. Hard-refresh Hub. Watchdog only watches gateway `~/.hermes/state.db` (misses profile DBs) — follow-up.
- **2026-07-21T22:45Z** — [PC] **Intent:** Hub stuck Loading (Machines & Sync / agent). Diagnose potato :8790 + hermes/state.db hang; restart if safe; evidence before claim.
- **2026-07-21T22:40Z** — [PC] **Intent:** Diagnose Tailscale→linuxbox failing when off home network. Evidence first (PC + potato status, MagDNS, exit-node, key expiry, DERP vs LAN direct). No destructive TS changes without ask.
- **2026-07-21T03:57Z** — [PC] **Result:** Potato Tasks projects restored (were `[]`). Merged 5 from PC: infranet, mazda3-sports-build, dashboard-ux-redesign, tableslop, linuxbox. Potato tasks left (2 done). Bak `user-tasks.json.bak.pre-projects-restore.20260721T035733Z`. API `user_projects` should show 5 — hard-refresh Tasks. Shutdown scheduled `/t 600`.
- **2026-07-21T03:56Z** — [PC] **Intent:** Tasks UI project dropdown empty on potato — potato `user-tasks.json` has `projects: []` (PC still has 5). Merge PC projects into potato by id; leave potato tasks untouched. Also: transfer done; shutdown `/t 600` (10 min).
- **2026-07-21T03:49Z** — [PC] **Intent:** Pixi **Hard regen** — distinct from soft Regenerate: modal edit last user (+ paren OOC), delete last assistant, clear discarded/anti-echo, Send as fresh generation. Potato SoT chat-ui only.
- **2026-07-21T03:27Z** — [PC] **Result:** Pixi per-step model pickers shipped on potato. Root override was chooseModelForTurn remapping Laguna→env FG (DeepSeek). Fixed sticky + added session.rpg.model_routing UI (primary/nsfw/continuation/quality/person_stub/cast_enrich). Precedence: step pick → session.model → env. Presets list Laguna+DeepSeek first-class. Revision 20260721-per-step-model-routing-v1; :8767 **200**.
- **2026-07-21T03:25Z** — [PC] **Intent:** Pixi regen similarity gate — user saw fail at Jaccard 0.40 after 4 hops; diagnose metric/threshold; loosen if too tight. Touch only regen similarity (not per-step model work).
- **2026-07-21T03:30Z** — [PC] **Result:** Regen gate = **bag-of-words Jaccard** (unique normalized words). Hard fail was **Jaccard ≥ 0.68** OR shared word-run (≥10 consec on regen) OR cocksleeve stall. **0.40 is NOT high** — Jaccard alone would pass; fail was almost certainly word-run while message still printed 0.40. Loosened on potato: regen word-run 10→14; shared-run only counts if Jaccard ≥ **CHAT_UI_REGEN_PHRASE_FLOOR** (default 0.50); Jaccard max env **CHAT_UI_REGEN_SIMILARITY_MAX** (default 0.68) via /api/config. Tests 4/4; pixi :8767 **200**; config shows sim_max=0.68 phrase_floor=0.5.

- **2026-07-21T03:15Z** — [PC] **Intent:** Pixi per-step model pickers — expand Primary control so user assigns Laguna/DeepSeek/Satyr/catalog models independently per real Send pipeline hop; sticky; precedence step	o session	o env. Potato SoT chat-ui.
- **2026-07-21T03:08Z** — [PC] **Result:** Pixi hallucination spiral fixed (rejection-meta + Options + SoC). Root: direction/quality/regen prompts demanded option menus + used "rejected" language that Laguna narrated ("Fresh start following rejection…"); hygiene missed it; continuation could concatenate SoC. Shipped: hygiene strip/reject rejection-meta, [Options]/Option Pivot, ~~spam, SoC walls; direction_contract + regen/quality/anti-OOC prompts = diegetic-only (no menus/rejection bait); quality hop falls back to prior clean. Tests 22 pass; revision 20260721-rejection-meta-hygiene-v1; pixi :8767 **200**.
-
- **2026-07-21T03:11Z** — [PC] **Intent:** Pixi FG model clarity — potato env still Laguna (`CHAT_UI_FG_OPENROUTER_MODEL=openrouter/poolside/laguna-xs-2.1:free`, `CHAT_UI_ALLOW_PAID_FALLBACK=0`) while sessions/UI prefer DeepSeek; switching FG default to `openrouter/deepseek/deepseek-v4-flash`, Laguna as free fallback, ALLOW_PAID=1 for main RP.
- **2026-07-21T03:12Z** — [PC] **Result:** Pixi FG switched Laguna→DeepSeek on potato. Was: `CHAT_UI_FG_OPENROUTER_MODEL=openrouter/poolside/laguna-xs-2.1:free` + `ALLOW_PAID_FALLBACK=0` (sessions already stored DeepSeek). Now: `WRITER_BOT_DEFAULT_MODEL`/`CHAT_UI_FG_OPENROUTER_MODEL=openrouter/deepseek/deepseek-v4-flash`, fallback Laguna free, `CHAT_UI_ALLOW_PAID_FALLBACK=1`. Restarted `linuxbox-pixi-rp`; log `OpenRouter default model: openrouter/deepseek/deepseek-v4-flash`; `:8767` **200**. Env bak: `~/.linuxbox-pixi/deckard-local.env.bak.pre-deepseek-fg.20260721T0311Z`.
- **2026-07-21T03:24Z** — [PC] **Intent:** Pixi paren→Turn guidance — `(...)` in composer = OOC/turn guidance (not diegetic); wire Send (+ regen reuse); tests; deploy potato chat-ui; avoid clobbering per-step model UI.
- **2026-07-21T03:28Z** — [PC] **Result:** Pixi paren→Turn guidance shipped on potato. `(...)` in composer → `turn_guidance` system layer (merged with Turn guidance field); diegetic bubble/API = remainder. Helper `splitParenTurnGuidance` in `session_turn_augment.mjs`; wired Send + regen; 7 tests pass; cache `20260721-paren-guidance-v1`; `linuxbox-pixi-rp` restarted; `:8767` **200**. No edit-user-message path in Pixi (N/A). Avoided per-step model UI.

- **2026-07-21T22:46Z** — [PC] **Intent:** Pixi English-only default — strengthen ENG directive + optional non-English detect/rewrite; `CHAT_UI_ENGLISH_ONLY=1` default on for FG Send + quality/continuation hops. Potato SoT chat-ui only; do not clobber hard-regen.

- **2026-07-21T22:51Z** — [PC] **Result:** Pixi English-only strengthened on potato SoT. Was: soft system tail only (models ignored); minimal recovery dropped ENG. Now: stronger `[Reply language — REQUIRED]` (English narration; short diegetic foreign OK); ENG re-attached on minimal retry; continuation/recovery nudges remind English; `response_hygiene.looks_like_non_english_wall` + `CHAT_UI_ENGLISH_ONLY_REJECT=1` blanks CJK-majority walls → existing diegetic rewrite. `CHAT_UI_ENGLISH_ONLY=1` default (unset). Tests 15/15; `linuxbox-pixi-rp` restarted; `:8767` **200**. No hard-regen touch.

- **2026-07-22T00:04Z** — [PC] **Intent:** Pixi list/roster truncation — user got mid-table garbage + `HAS_TRUNCATED_CONTENT` on Candy/Vault30 roster ask. Investigate potato chat-ui continuation/hygiene/turn_max_tokens; fix auto-continue for list turns; strip marker; deploy.

- **2026-07-22T00:15Z** — [PC] **Result:** Pixi list/roster truncation fixed on potato. Root: DeepSeek `finish_reason=stop` after ~375 toks mid-table + `*hijabi* (headscarf)` loop + model/UI `_HAS_TRUNCATED_CONTENT_` marker; server auto-continue only watched length/hanging-tail so it never fired; client did 1 hop then quality. Shipped: incomplete-table detect + list multi-hop continue (server+client); strip marker + collapse paren spam; list turn bump `CHAT_UI_LIST_TURN_MAX_TOKENS` default 6144; envs `CHAT_UI_LIST_MAX_CONTINUATIONS`/`LIST_TOTAL_MAX_TOKENS`/`LIST_CONTINUATION_MAX_TOKENS`. Tests 31/31; revision `20260722-list-roster-continue-v1`; `:8767` **200**. Hard-refresh Pixi; re-ask full dossier.

- **2026-07-24T00:30Z** — [PC] **Intent:** Refine think guardrails — softer security *checks* (passable gates) vs rigid bans that strand work; teach agent how to pass; fix status thrash. Holder: think-guardrails-refine.

- **2026-07-24T00:32Z** — [PC] **Intent:** Fold UI-verify (Playwright one-shot) into think C-checks — not curl-only when human reports UI fail; no Hermes browser_navigate. Holder: think-guardrails-refine.

- **2026-07-24T00:45Z** — [PC] **Result:** Think guardrails → passable checks C0–C7. Soft walls: C0 hard-deny only; C1 form+yolo; C2 curl/systemctl; C3 no Hermes browser (Firecrawl cloud OK); C4 status enforce-status safety net (verified fb1fe2d8→blocked); C5 soft git; **C6 UI** `run-chat-ui-smoke.sh` (Playwright one-shot, loopback `/`) — PASS on potato (`reports/chat-ui-smoke/LATEST.md`); **C7 turns** soft~12 / hard **18** default / **24** ops-UI, timeouts 210/270s. Docs: `agents/THINK_SECURITY_CHECKS.md` + `docs/ops-think-shell-access.md`. Deployed `~/bin/agent-cycle-think-tick.sh`. :8790 200. No commit.
- **2026-07-25T03:26Z** — [PC] **Result:** Resumed think tick on potato — was paused (`agents/state/think-paused.json`, Step $14/day burn fix). Verified think profile FREE-ONLY (Laguna→Nemotron→Ling `:free`, no Step) + `THINK_INTERVAL_SEC=480`, then ran `~/bin/resume-agent-ticks-adaptive.sh`; crontab now `* * * * * ~/bin/agent-cycle-think-tick.sh # agent-cycle-think-adaptive`. Pause flag marked resumed (kept as history). First tick 23:20 ET picked **tableslop ts-a** (`think-focus.json` running, free-first). No commit.
- **2026-07-25T03:45Z** — [PC] **Result:** Fixed 404-model Hermes daily jobs on potato. Root: root `~/.hermes/config.yaml` default chain was dead `openrouter/owl-alpha` → `moonshotai/kimi-k2.6:free` (both MISSING on OpenRouter probe); profile-less cron jobs (situation-hermes, site-abhinavall-review, spacequest/nyc-mafia worldbuilding reviews) inherited it — `hermes-model-failover.sh` only patches profile configs, never root. Now: default `poolside/laguna-xs-2.1:free`, fallback `inclusionai/ling-3.0-flash:free` (both LIVE); bak `config.yaml.bak-20260725`. Removed duplicate `situation-rss` cron `2242702085a0` (Jul-4 copy; kept original `8fb495296cd1`). `hermes-gateway` restarted, active; config greps 0 dead slugs. Known leftovers: situation-rss exits 141 (SIGPIPE, brief still written OK); site-abhinavall-ping 127 on missing `~/.hermes/scripts/abhinavall_cleanup.sh`. No commit.
- **2026-07-25T03:32Z** — [PC] **Result:** site-abhinavall-ping exit 127: `abhinavall-check.sh` called `abhinavall_cleanup.sh` but install named it `abhinavall-cleanup.sh`; symlink `~/.hermes/scripts/abhinavall_cleanup.sh` → cleanup; ping EXIT:0. No commit.

- **2026-07-25T03:50Z** — [PC] **Result:** Fixed 90s Hub 502 restart hang on `linuxbox-status`. Root: unit had default `TimeoutStopSec=90s`; server blocks event loop in `execFileSync("ps")` (`hermesGatewayUp` D-state probe) — when `ps` hangs in D-state its SIGTERM timeout kill never lands, node stays blocked, systemd sits in final-sigterm 90s. Fix: unit now `KillMode=control-group` + `TimeoutStopSec=10` + `SendSIGKILL=yes`; `ps` exec gets `killSignal: "SIGKILL"` (box + PC copies in parity, uncommitted). daemon-reload + one controlled restart: **0.124s**. Verified `/` `/api/agent` `/api/systems` `/api/machines` `/api/model-budget` all 200 on :8790. No commit.
- **2026-07-25T03:58Z** — [PC] **Intent:** Equalize think picker: campaigns + product boards same tier (RR after [ops]); work_pipeline health on /api/agent; deploy ~/bin; verify has-work+picker. No commit.
- **2026-07-25T04:02Z** — [PC] **Result:** Think picker equalized: after `[ops]`, **same-tier RR** product boards (tableslop/pixi/portfolio) + campaign progress (nyc/tropic) via `CONTINUOUS_LANES` + `agents/state/think-continuous-rr.json` (enforce-lane flip unchanged). Seeded `agents/portfolio-progress.md` (pf-a/b). `/api/agent` **work_pipeline** (open counts + last think success/attempt + stalled). Deployed `~/bin` + repo tick scripts + server `db-20260725-workpipe-r1`. Verify: has-work WORK; RR sequence tableslop→pixi→portfolio→nyc→tropic; work_pipeline stalled=true (last status failed, no done). Priority: ops → product+campaign RR → product tasks → other tasks → other markers. No commit.

- **2026-07-25T15:58Z** — [PC] **Intent:** Diagnose linuxbox not working. SSH potato first; then units/curl/disk/journal; safe one-step fix if clear. No wipe/no blind restart. No commit.

- **2026-07-25T16:00Z** — [PC] **Result:** linuxbox check-in. SSH potato **OK** (up 4d, load~1.7→4 during fix). Loopback **200**: :8790 :3000 :8765/health :8767. Edge **200**: abhinavall.net + map.tableslop.org; /Linuxbox/ **401** (auth expected). Units active: linuxbox-status, tableslop, abhinav-portfolio, both cloudflared, user hermes-gateway + pixi. Broken: **hermes-gateway-hunter-reckoning** D-state ~2d after Discord **Improper token**/401; restarted then immediately D again (folio_wait) — **stopped** until token fixed (unit inactive). Also: OpenRouter free Laguna **429** free-models-per-day-high-balance (~10:00); think state.db **311M**; swap 46% used; dphys-swapfile failed (alt 2G swap OK). Disk root 32% free. Crontab fast+think present. No wipe/no commit.

- **2026-07-25T16:00Z** — [PC] **Intent:** Archive bloated Hermes think profile state.db (~311M D-state risk) on potato to /mnt/archive/logs/; leave Hermes recreate; do not touch Hunter token/unit. No commit.

- **2026-07-25T16:02Z** — [PC] **Result:** Archived Hermes think state.db on potato. Old **311M** (325521408B) → `/mnt/archive/logs/hermes-state-db-20260725T1601Z/think-state.db` (+wal/shm). Fresh think `state.db` recreated **0→4.0K** after `hermes -p think status` OK. `hermes-gateway` **active**; Hunter unit left **inactive** (no token touch). Guard: `HERMES_PROFILE_DB_GUARD_LIST=think hermes-profile-db-guard.sh`. Root disk 32%→31%. No commit.

- **2026-07-25T16:14:57Z** — [PC] **Intent:** Hub Active Now idle+429 on PIXI progress — diagnose think-focus/picker/429 failover + Active Now copy; fix unstick/inbox/UI. Holder: active-now-429-diag.

- **2026-07-25T16:22:12Z** — [PC] **Result:** Hub Active Now idle+429 diagnosed+fixed. Root: (1) think continuous RR preferred PIXI/portfolio over 17 open user-tasks (Mazda3/Infranet not blockers); (2) OpenRouter free-models-per-day-high-balance on Laguna — Hermes retried same model 3× (fallback_providers no-op same-provider) exit 0 so Hub showed done/ok; (3) Active Now "Nothing running" = no in-flight process (correct between ticks) but looked dead. Fixes: think tick free-chain `-m` rotate Laguna→Nemotron→Ling, demote lane on 429, preserve attempt counts across boards, skip LLM + post inbox when all free blocked; focus failed not fake-done; Hub copy Idle-between-ticks/open counts/last/429; dash `db-20260725-activenow-429-r1`. Inbox **think-free-429-20260725** (reset ~2026-07-26T00:00Z). Potato deployed; verify blocked gate skips Hermes. No commit.

- **2026-07-26T02:07:15Z** — [PC] **Intent:** Pixi RP kit permanence + decide-dont-ask + injection packs. Patch potato `~/pixi-rp` chat-ui: (1) clothing-change beats OVERWRITE `current_outfit` (was fill-blank-only) + salvage equipped weapons/armor into inventory; (2) present-cast-state demands WORLD_DELTA kit rows the turn kit changes; (3) new relevance-gated `INJECTION_PACKS` (kit_gear / world_storage / system_dynamics / mechanics, max 2 per turn, session pin/mute override); (4) `decision_commit` layer so NPCs decide instead of asking the player questions. Rev `20260725-kit-packs-v1`. No registry/state writes.

- **2026-07-26T02:09:40Z** — [PC] **Result:** Pixi kit permanence + packs shipped to potato, rev `20260725-kit-packs-v1` (`linuxbox-pixi-rp` active, `/api/config` confirms). (1) Salvage now OVERWRITES `current_outfit` on change verbs (changes/pulls/puts on, slips/shrugs/steps into, swaps, now wearing) and still fill-blank-only on passive "wearing" mentions; equipped weapons/armor (holsters/straps on/dons/slings) merge into `inventory` with weapon|armor|gear slot. (2) Present-cast-state block now demands WORLD_DELTA kit rows the turn kit changes. (3) `INJECTION_PACKS` = kit_gear / world_storage / system_dynamics / mechanics, relevance-gated (>=2 keyword hits), **max 2 per turn**, override via `rpg.injection_packs` (array = force, or {pin,mute}), scenario text via `rpg.injection_pack_notes[id]`. (4) `decision_commit` layer: NPCs decide+act, no questions back unless player QUOTED a direct question. Self-check `tests/check_kit_packs.mjs` -> KIT_PACKS_CHECK_OK (6 cases incl. no-clobber-on-passive + cap + pin/mute). Backup `~/.pixi-patch-backup/20260725-kit-packs/`. **Not done:** active session `c5409afe` still has 13 people / all outfits blank / 4 objects — backfill NOT run, awaiting user OK.
- **2026-07-26T02:18Z** — [PC] **Intent:** tableslop map: gold region marker 2 wrongly labeled Porto Lujuria (should be Sierra Dorado); replace circle overlay with gold jagged polygon; add paste/highlight feedback → user-task. Deploy potato :8765. No commit.

- **2026-07-26T02:19:26Z** — [PC] **Intent:** Pixi follow-up: (1) dry-run kit backfill for session c5409afe from prose (write nothing); (2) Studio tab pin/mute toggles for injection packs. No registry writes.

- **2026-07-26T02:22:31Z** — [PC] **Result:** Dry-run kit backfill for c5409afe (write nothing) + Studio packs UI live rev `20260725-kit-packs-v2`. Dry-run curated (msg#94 strongest): Emily=`dark wash skinny jeans` + `soft gray oversized sweater` + black lace bralette edge; Mia=`black high-waisted shorts` + `cropped white tank` + `sheer black mesh long-sleeve` + `plunge bodysuit`. Auto-regex alone too noisy for gear — curated apply recommended next. Studio: pin/mute per pack, last-fired badge, Clear overrides. Hard-refresh :8767 Studio tab.

- **2026-07-26T02:22:46Z** — [PC] **Intent:** Continue Pixi injection-packs development via Gemini Notebook + ingestion handoff. Draft pack-domain prompts for user to run; ingest returned artifacts into potato packs. No registry writes.

- **2026-07-26T02:24:44Z** — [PC] **Result:** Pixi injection-packs Gemini/NotebookLM handoff scaffolded under `docs/pixi/injection-packs/` (README, CONTRACT, prompts 00–04, sources/LINKS, returns/). Ingest script `scripts/pixi/ingest-injection-pack.py` (dry-run → staging JSON). Workflow: user runs NotebookLM with Setup+pack prompt + sources → drops return → agent ingests. Recommended first pack: kit_gear. Awaiting user links + returned JSON.
